{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Minimal P2 grant: the two app commands of the typed local bridge, nothing else.",
  "windows": ["main"],
  "permissions": ["allow-desktop-info", "allow-bridge-request"]
}
