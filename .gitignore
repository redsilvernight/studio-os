# Claude Code local state (machine-specific, not shared)
.claude/settings.local.json
.claude/settings.local.backup.json
.claude/scheduled_tasks.lock
.claude/worktrees/
*.graphify-bak
*.backup-*

# Graphify generated output (rebuildable, excluded from vault/git per convention)
graphify-out/

# OS/editor cruft
.DS_Store
Thumbs.db
*.swp

# Backend/client stacks (Bloc A/B)
__pycache__/
*.pyc
.venv/
venv/
node_modules/
.env
.ruff_cache/
.mypy_cache/
.pytest_cache/
*.egg-info/
dist/
build/
test-results/
playwright-report/
output/

# Installateur Desktop copié à la racine pour distribution interne (jamais commité)
/StudiOS-Setup-*.exe

# Docker local volumes (dev compose)
docker/.data/

# Dumps PostgreSQL (backup.sh) : contiennent des hashes de credentials, jamais commites
docker/backups/
