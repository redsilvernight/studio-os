{
  "$defs": {
    "ProtocolVersion": {
      "additionalProperties": false,
      "properties": {
        "major": {
          "maximum": 999,
          "minimum": 1,
          "title": "Major",
          "type": "integer"
        },
        "minor": {
          "maximum": 999,
          "minimum": 0,
          "title": "Minor",
          "type": "integer"
        }
      },
      "required": [
        "major",
        "minor"
      ],
      "title": "ProtocolVersion",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "properties": {
    "maximum": {
      "$ref": "#/$defs/ProtocolVersion"
    },
    "minimum": {
      "$ref": "#/$defs/ProtocolVersion"
    }
  },
  "required": [
    "minimum",
    "maximum"
  ],
  "title": "ProtocolRange",
  "type": "object"
}
