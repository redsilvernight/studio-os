{
 "openapi": "3.1.0",
 "info": {
  "title": "Studio OS API",
  "description": "Shared coordination API for Studio OS projects: tasks, resource claims, work sessions, decisions, AI work logs, events, and file transfer metadata. Every operation under `/api/v1` requires machine authentication (`Authorization: Bearer <machine-token>`, provisioned out of band) except `GET /healthz`, `GET /metrics` and the human dashboard login `POST /auth/token`. File bytes never flow through this API \u2014 transfers exchange metadata and short-lived signed URLs only, uploads and downloads go directly to object storage. Replayable creations accept `Idempotency-Key`; concurrent updates use `If-Match-Version`; permission refusals answer `403 forbidden` and are final.",
  "version": "1"
 },
 "paths": {
  "/healthz": {
   "get": {
    "tags": [
     "health"
    ],
    "summary": "Healthz",
    "description": "Liveness probe. Needs no credential and carries no security requirement \u2014 one of the unauthenticated operations, alongside `GET /metrics` and the human dashboard login `POST /auth/token`. Answers `{\"status\": \"ok\"}` when the service is up; use it before authenticating anything else.",
    "operationId": "healthz_healthz_get",
    "responses": {
     "200": {
      "description": "The service is up.",
      "content": {
       "application/json": {
        "schema": {
         "additionalProperties": {
          "type": "string"
         },
         "type": "object",
         "title": "Response Healthz Healthz Get"
        },
        "example": {
         "status": "ok"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/auth/token": {
   "post": {
    "tags": [
     "auth"
    ],
    "summary": "Login",
    "description": "Exchange human credentials for a short-lived dashboard JWT.",
    "operationId": "login_api_v1_auth_token_post",
    "requestBody": {
     "content": {
      "application/json": {
       "schema": {
        "$ref": "#/components/schemas/TokenRequest"
       }
      }
     },
     "required": true
    },
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/TokenResponse"
        }
       }
      }
     },
     "401": {
      "description": "Invalid email or password."
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/projects": {
   "get": {
    "tags": [
     "projects"
    ],
    "summary": "List Projects",
    "description": "List all projects. Any authenticated machine may read.",
    "operationId": "list_projects_api_v1_projects_get",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "type": "array",
         "items": {
          "$ref": "#/components/schemas/Project"
         },
         "title": "Response List Projects Api V1 Projects Get"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     }
    }
   },
   "post": {
    "tags": [
     "projects"
    ],
    "summary": "Create Project",
    "description": "Create a project. Requires a privileged role (admin or developer); other roles receive `403 forbidden`. Slugs are unique: reusing one fails with 409. Accepts `Idempotency-Key` for safe retries.",
    "operationId": "create_project_api_v1_projects_post",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "Idempotency-Key",
      "in": "header",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string"
        },
        {
         "type": "null"
        }
       ],
       "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`.",
       "title": "Idempotency-Key"
      },
      "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`."
     }
    ],
    "requestBody": {
     "required": true,
     "content": {
      "application/json": {
       "schema": {
        "$ref": "#/components/schemas/ProjectCreate"
       }
      }
     }
    },
    "responses": {
     "201": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/Project"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "409": {
      "description": "Replay key problem, no duplicate was created: either the same `Idempotency-Key` was reused with a different body (`idempotency_key_payload_mismatch` \u2014 resend the exact original body) or a previous creation with this key is still completing (`idempotency_key_in_progress` \u2014 retry identically after a short delay).",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "idempotency_key_payload_mismatch"
         }
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/projects/{project_id}": {
   "get": {
    "tags": [
     "projects"
    ],
    "summary": "Get Project",
    "description": "Get one project by id. Any authenticated machine may read.",
    "operationId": "get_project_api_v1_projects__project_id__get",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "project_id",
      "in": "path",
      "required": true,
      "schema": {
       "type": "string",
       "format": "uuid",
       "title": "Project Id"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/Project"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "404": {
      "description": "No such resource. Unknown ids return 404; access to an existing but unauthorized transfer returns 403 instead, never 404.",
      "content": {
       "application/json": {
        "example": {
         "detail": "task not found"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/projects/{project_id}/state": {
   "get": {
    "tags": [
     "projects"
    ],
    "summary": "Get Project State",
    "description": "Bootstrap read for a project: its active tasks and active resource claims in one call. Start here before working on a project. Any authenticated machine may read.",
    "operationId": "get_project_state_api_v1_projects__project_id__state_get",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "project_id",
      "in": "path",
      "required": true,
      "schema": {
       "type": "string",
       "format": "uuid",
       "title": "Project Id"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/ProjectState"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "404": {
      "description": "No such resource. Unknown ids return 404; access to an existing but unauthorized transfer returns 403 instead, never 404.",
      "content": {
       "application/json": {
        "example": {
         "detail": "task not found"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/tasks": {
   "get": {
    "tags": [
     "tasks"
    ],
    "summary": "List Tasks",
    "description": "List tasks, optionally filtered by project. Any authenticated machine may read.",
    "operationId": "list_tasks_api_v1_tasks_get",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "project_id",
      "in": "query",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string",
         "format": "uuid"
        },
        {
         "type": "null"
        }
       ],
       "title": "Project Id"
      }
     },
     {
      "name": "limit",
      "in": "query",
      "required": false,
      "schema": {
       "type": "integer",
       "maximum": 500,
       "default": 100,
       "title": "Limit"
      }
     },
     {
      "name": "offset",
      "in": "query",
      "required": false,
      "schema": {
       "type": "integer",
       "default": 0,
       "title": "Offset"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "type": "array",
         "items": {
          "$ref": "#/components/schemas/Task"
         },
         "title": "Response List Tasks Api V1 Tasks Get"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   },
   "post": {
    "tags": [
     "tasks"
    ],
    "summary": "Create Task",
    "description": "Create a task. Requires a writer role. Accepts `Idempotency-Key` for safe retries: the same key with the identical body returns the original task instead of a duplicate.",
    "operationId": "create_task_api_v1_tasks_post",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "Idempotency-Key",
      "in": "header",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string"
        },
        {
         "type": "null"
        }
       ],
       "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`.",
       "title": "Idempotency-Key"
      },
      "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`."
     }
    ],
    "requestBody": {
     "required": true,
     "content": {
      "application/json": {
       "schema": {
        "$ref": "#/components/schemas/TaskCreate"
       }
      }
     }
    },
    "responses": {
     "201": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/Task"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "409": {
      "description": "Replay key problem, no duplicate was created: either the same `Idempotency-Key` was reused with a different body (`idempotency_key_payload_mismatch` \u2014 resend the exact original body) or a previous creation with this key is still completing (`idempotency_key_in_progress` \u2014 retry identically after a short delay).",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "idempotency_key_payload_mismatch"
         }
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/tasks/{task_id}": {
   "get": {
    "tags": [
     "tasks"
    ],
    "summary": "Get Task",
    "description": "Get one task by id. Any authenticated machine may read.",
    "operationId": "get_task_api_v1_tasks__task_id__get",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "task_id",
      "in": "path",
      "required": true,
      "schema": {
       "type": "string",
       "format": "uuid",
       "title": "Task Id"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/Task"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "404": {
      "description": "No such resource. Unknown ids return 404; access to an existing but unauthorized transfer returns 403 instead, never 404.",
      "content": {
       "application/json": {
        "example": {
         "detail": "task not found"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   },
   "patch": {
    "tags": [
     "tasks"
    ],
    "summary": "Update Task",
    "description": "Update a task's title, description or status. Requires a writer role and the current `If-Match-Version`; a stale version is rejected with the live server version instead of overwriting.",
    "operationId": "update_task_api_v1_tasks__task_id__patch",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "task_id",
      "in": "path",
      "required": true,
      "schema": {
       "type": "string",
       "format": "uuid",
       "title": "Task Id"
      }
     },
     {
      "name": "If-Match-Version",
      "in": "header",
      "required": true,
      "schema": {
       "type": "integer",
       "description": "Optimistic concurrency guard, required. Send the `version` value last read for the object (from any GET response). If another writer changed the object first, the update is rejected with `409 version_conflict` carrying the current server version \u2014 re-read, merge, and retry. Updates never overwrite silently.",
       "title": "If-Match-Version"
      },
      "description": "Optimistic concurrency guard, required. Send the `version` value last read for the object (from any GET response). If another writer changed the object first, the update is rejected with `409 version_conflict` carrying the current server version \u2014 re-read, merge, and retry. Updates never overwrite silently."
     }
    ],
    "requestBody": {
     "required": true,
     "content": {
      "application/json": {
       "schema": {
        "$ref": "#/components/schemas/TaskUpdate"
       }
      }
     }
    },
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/Task"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "404": {
      "description": "No such resource. Unknown ids return 404; access to an existing but unauthorized transfer returns 403 instead, never 404.",
      "content": {
       "application/json": {
        "example": {
         "detail": "task not found"
        }
       }
      }
     },
     "409": {
      "description": "Stale `If-Match-Version`: another writer changed the object first. `server_version` is the current version \u2014 re-read the object, merge, and retry with the new version.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "version_conflict",
          "server_version": 3
         }
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/tasks/{task_id}/claim": {
   "post": {
    "tags": [
     "tasks"
    ],
    "summary": "Claim Task",
    "description": "Claim a task for the caller's machine (sets status to in_progress). Requires a writer role. Fails with `already_claimed` if another machine holds it.",
    "operationId": "claim_task_api_v1_tasks__task_id__claim_post",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "task_id",
      "in": "path",
      "required": true,
      "schema": {
       "type": "string",
       "format": "uuid",
       "title": "Task Id"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/Task"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "404": {
      "description": "No such resource. Unknown ids return 404; access to an existing but unauthorized transfer returns 403 instead, never 404.",
      "content": {
       "application/json": {
        "example": {
         "detail": "task not found"
        }
       }
      }
     },
     "409": {
      "description": "Another machine already holds this task's claim (soft lock). Release by its owner, or pick another task \u2014 claims warn, they never queue.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "already_claimed"
         }
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/tasks/{task_id}/release": {
   "post": {
    "tags": [
     "tasks"
    ],
    "summary": "Release Task",
    "description": "Release a task's claim. Only the machine holding the claim (or a privileged role) may release it; anyone else receives `403 forbidden`.",
    "operationId": "release_task_api_v1_tasks__task_id__release_post",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "task_id",
      "in": "path",
      "required": true,
      "schema": {
       "type": "string",
       "format": "uuid",
       "title": "Task Id"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/Task"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "404": {
      "description": "No such resource. Unknown ids return 404; access to an existing but unauthorized transfer returns 403 instead, never 404.",
      "content": {
       "application/json": {
        "example": {
         "detail": "task not found"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/sessions": {
   "get": {
    "tags": [
     "sessions"
    ],
    "summary": "List Sessions",
    "description": "List work sessions, optionally filtered by task. Any authenticated machine may read.",
    "operationId": "list_sessions_api_v1_sessions_get",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "task_id",
      "in": "query",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string",
         "format": "uuid"
        },
        {
         "type": "null"
        }
       ],
       "title": "Task Id"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "type": "array",
         "items": {
          "$ref": "#/components/schemas/WorkSession"
         },
         "title": "Response List Sessions Api V1 Sessions Get"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   },
   "post": {
    "tags": [
     "sessions"
    ],
    "summary": "Start Session",
    "description": "Start a work session on a task for the caller's machine. Requires a writer role. Accepts `Idempotency-Key` for safe retries: the same key returns the original session instead of starting a duplicate.",
    "operationId": "start_session_api_v1_sessions_post",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "Idempotency-Key",
      "in": "header",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string"
        },
        {
         "type": "null"
        }
       ],
       "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`.",
       "title": "Idempotency-Key"
      },
      "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`."
     }
    ],
    "requestBody": {
     "required": true,
     "content": {
      "application/json": {
       "schema": {
        "$ref": "#/components/schemas/WorkSessionCreate"
       }
      }
     }
    },
    "responses": {
     "201": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/WorkSession"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "409": {
      "description": "Replay key problem, no duplicate was created: either the same `Idempotency-Key` was reused with a different body (`idempotency_key_payload_mismatch` \u2014 resend the exact original body) or a previous creation with this key is still completing (`idempotency_key_in_progress` \u2014 retry identically after a short delay).",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "idempotency_key_payload_mismatch"
         }
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/sessions/{session_id}/end": {
   "patch": {
    "tags": [
     "sessions"
    ],
    "summary": "End Session",
    "description": "End a work session. Only the machine that started the session (or a privileged role) may end it; ending twice is a harmless no-op, never an error storm.",
    "operationId": "end_session_api_v1_sessions__session_id__end_patch",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "session_id",
      "in": "path",
      "required": true,
      "schema": {
       "type": "string",
       "format": "uuid",
       "title": "Session Id"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/WorkSession"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "404": {
      "description": "No such resource. Unknown ids return 404; access to an existing but unauthorized transfer returns 403 instead, never 404.",
      "content": {
       "application/json": {
        "example": {
         "detail": "task not found"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/claims": {
   "get": {
    "tags": [
     "claims"
    ],
    "summary": "List Claims",
    "description": "List resource claims, optionally filtered by project. Any authenticated machine may read.",
    "operationId": "list_claims_api_v1_claims_get",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "project_id",
      "in": "query",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string",
         "format": "uuid"
        },
        {
         "type": "null"
        }
       ],
       "title": "Project Id"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "type": "array",
         "items": {
          "$ref": "#/components/schemas/ResourceClaim"
         },
         "title": "Response List Claims Api V1 Claims Get"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   },
   "post": {
    "tags": [
     "claims"
    ],
    "summary": "Create Claim",
    "description": "Claim a resource path (file or folder) for the caller's machine. Claims are soft locks with a TTL: they warn other machines through a conflict event when paths overlap, but never block a write, a Git operation, or a transfer. Requires a writer role. Accepts `Idempotency-Key` for safe retries \u2014 replaying the same key never re-emits the conflict event.",
    "operationId": "create_claim_api_v1_claims_post",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "Idempotency-Key",
      "in": "header",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string"
        },
        {
         "type": "null"
        }
       ],
       "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`.",
       "title": "Idempotency-Key"
      },
      "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`."
     }
    ],
    "requestBody": {
     "required": true,
     "content": {
      "application/json": {
       "schema": {
        "$ref": "#/components/schemas/ResourceClaimCreate"
       }
      }
     }
    },
    "responses": {
     "201": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/ResourceClaim"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "409": {
      "description": "Replay key problem, no duplicate was created: either the same `Idempotency-Key` was reused with a different body (`idempotency_key_payload_mismatch` \u2014 resend the exact original body) or a previous creation with this key is still completing (`idempotency_key_in_progress` \u2014 retry identically after a short delay).",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "idempotency_key_payload_mismatch"
         }
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/claims/{claim_id}/renew": {
   "post": {
    "tags": [
     "claims"
    ],
    "summary": "Renew Claim",
    "description": "Extend a claim's TTL. Only the machine holding the claim (or a privileged role) may renew it.",
    "operationId": "renew_claim_api_v1_claims__claim_id__renew_post",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "claim_id",
      "in": "path",
      "required": true,
      "schema": {
       "type": "string",
       "format": "uuid",
       "title": "Claim Id"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/ResourceClaim"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "404": {
      "description": "No such resource. Unknown ids return 404; access to an existing but unauthorized transfer returns 403 instead, never 404.",
      "content": {
       "application/json": {
        "example": {
         "detail": "task not found"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/claims/{claim_id}": {
   "delete": {
    "tags": [
     "claims"
    ],
    "summary": "Release Claim",
    "description": "Release a resource claim. Only the machine holding the claim (or a privileged role) may release it. Releasing twice is harmless \u2014 never a duplicate problem.",
    "operationId": "release_claim_api_v1_claims__claim_id__delete",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "claim_id",
      "in": "path",
      "required": true,
      "schema": {
       "type": "string",
       "format": "uuid",
       "title": "Claim Id"
      }
     }
    ],
    "responses": {
     "204": {
      "description": "Successful Response"
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "404": {
      "description": "No such resource. Unknown ids return 404; access to an existing but unauthorized transfer returns 403 instead, never 404.",
      "content": {
       "application/json": {
        "example": {
         "detail": "task not found"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/decisions": {
   "get": {
    "tags": [
     "decisions"
    ],
    "summary": "List Decisions",
    "description": "List recorded decisions, optionally filtered by project. Any authenticated machine may read.",
    "operationId": "list_decisions_api_v1_decisions_get",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "project_id",
      "in": "query",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string",
         "format": "uuid"
        },
        {
         "type": "null"
        }
       ],
       "title": "Project Id"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "type": "array",
         "items": {
          "$ref": "#/components/schemas/Decision"
         },
         "title": "Response List Decisions Api V1 Decisions Get"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   },
   "post": {
    "tags": [
     "decisions"
    ],
    "summary": "Create Decision",
    "description": "Record a project decision. Requires a writer role; the proposer identity is derived from the caller's authenticated machine. Accepts `Idempotency-Key` for safe retries: the same key returns the original decision instead of allocating a second id.",
    "operationId": "create_decision_api_v1_decisions_post",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "Idempotency-Key",
      "in": "header",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string"
        },
        {
         "type": "null"
        }
       ],
       "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`.",
       "title": "Idempotency-Key"
      },
      "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`."
     }
    ],
    "requestBody": {
     "required": true,
     "content": {
      "application/json": {
       "schema": {
        "$ref": "#/components/schemas/DecisionCreate"
       }
      }
     }
    },
    "responses": {
     "201": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/Decision"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "409": {
      "description": "Replay key problem, no duplicate was created: either the same `Idempotency-Key` was reused with a different body (`idempotency_key_payload_mismatch` \u2014 resend the exact original body) or a previous creation with this key is still completing (`idempotency_key_in_progress` \u2014 retry identically after a short delay).",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "idempotency_key_payload_mismatch"
         }
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/library": {
   "get": {
    "tags": [
     "library"
    ],
    "summary": "List Library",
    "description": "List library definitions, optionally filtered. User-scope rows are visible to their owner (or an admin) only \u2014 collections never count, list, or hint at another user's private resources.",
    "operationId": "list_library_api_v1_library_get",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "kind",
      "in": "query",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string"
        },
        {
         "type": "null"
        }
       ],
       "title": "Kind"
      }
     },
     {
      "name": "scope",
      "in": "query",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string"
        },
        {
         "type": "null"
        }
       ],
       "title": "Scope"
      }
     },
     {
      "name": "project_id",
      "in": "query",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string",
         "format": "uuid"
        },
        {
         "type": "null"
        }
       ],
       "title": "Project Id"
      }
     },
     {
      "name": "limit",
      "in": "query",
      "required": false,
      "schema": {
       "type": "integer",
       "maximum": 500,
       "default": 100,
       "title": "Limit"
      }
     },
     {
      "name": "offset",
      "in": "query",
      "required": false,
      "schema": {
       "type": "integer",
       "default": 0,
       "title": "Offset"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "type": "array",
         "items": {
          "$ref": "#/components/schemas/LibraryResource"
         },
         "title": "Response List Library Api V1 Library Get"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   },
   "post": {
    "tags": [
     "library"
    ],
    "summary": "Create Library Resource",
    "description": "Create a library definition plus its draft version 1 (which never activates itself). Studio scope requires a privileged role; project scope requires its `project_id`; user scope is owned by the caller. Accepts `Idempotency-Key` for safe retries.",
    "operationId": "create_library_resource_api_v1_library_post",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "Idempotency-Key",
      "in": "header",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string"
        },
        {
         "type": "null"
        }
       ],
       "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`.",
       "title": "Idempotency-Key"
      },
      "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`."
     }
    ],
    "requestBody": {
     "required": true,
     "content": {
      "application/json": {
       "schema": {
        "$ref": "#/components/schemas/LibraryResourceCreate"
       }
      }
     }
    },
    "responses": {
     "201": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/LibraryResource"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "409": {
      "description": "Replay key problem, no duplicate was created: either the same `Idempotency-Key` was reused with a different body (`idempotency_key_payload_mismatch` \u2014 resend the exact original body) or a previous creation with this key is still completing (`idempotency_key_in_progress` \u2014 retry identically after a short delay). Library reference conflict: the `stable_key` is already taken in this scope (`duplicate_stable_key`); a dependency pin names several visible resources (`pin_ambiguous` \u2014 disambiguate the pin); or a lock already exists for this `(project, resource)` pair (`already_locked`).",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "idempotency_key_payload_mismatch"
         }
        }
       }
      }
     },
     "404": {
      "description": "Library reference not found: unknown id, or no visible resource matches a dependency pin (`pin_not_found`), or the pinned version was never created (`pin_version_not_found` / `version_not_found`). Pins on another user's private resources answer the same 404, never a hint of their existence.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "pin_not_found",
          "stable_key": "..."
         }
        }
       }
      }
     },
     "422": {
      "description": "Library scope context rejected, nothing stored: project scope requires `project_id`, studio and user scopes forbid it (`invalid_scope_context`). Library semantic content rejected, nothing stored (P3 semantic content): the version `content` must match its per-kind schema (`content_schema: studio.library.<kind>/v1`, unknown fields forbidden, `rule`/`skill` prose bounded to 65_536 characters, `model_profile` carrying vendor-neutral `requirements` only, `agent_definition` descriptive only, `workflow` declarative only with `participants`, `inputs`/`outputs` and no runtime field). Per-field `details` describe the caller's own payload only. Workflow definition rejected, nothing stored (P11 declarative workflow): the version `content` is schema-valid but structurally incoherent \u2014 duplicate `participant_id` (`duplicate_participant`), a `depends_on` naming no participant of this workflow (`unknown_dependency`), a dependency cycle (`dependency_cycle`), a participant `agent_stable_key` with no matching `composes_agent` pin (`unknown_participant_agent`), a `composes_agent` pin no participant uses (`unused_agent_dependency`), a duplicated I/O name (`duplicate_io_declaration`), or a dataflow `source` that does not resolve to a declared participant output / workflow input (`invalid_io_reference`). Static validation only \u2014 it decides no execution, schedules nothing and stores no run state; raised after the auth/scope gates and before the 404/409 pin gates, never an oracle on invisible resources. Library binding rejected, nothing stored (P5 typed bindings): the dependency pin names a forbidden kind couple (`forbidden_kind_pair`), an explicit relation that does not match the couple (`relation_mismatch`), a second model profile on one agent definition (`too_many_model_profiles`), twice the same target (`duplicate_binding`), or a private user target from a shared definition (`forbidden_scope`). Raised only after the 404/409 existence gates \u2014 never an oracle on invisible resources.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "invalid_scope_context"
         }
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/library/{resource_id}": {
   "get": {
    "tags": [
     "library"
    ],
    "summary": "Get Library Resource",
    "description": "Get one library definition. A User-scope definition owned by someone else answers 404, never 403, so its existence cannot be inferred.",
    "operationId": "get_library_resource_api_v1_library__resource_id__get",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "resource_id",
      "in": "path",
      "required": true,
      "schema": {
       "type": "string",
       "format": "uuid",
       "title": "Resource Id"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/LibraryResource"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "404": {
      "description": "No such resource. Unknown ids return 404; access to an existing but unauthorized transfer returns 403 instead, never 404.",
      "content": {
       "application/json": {
        "example": {
         "detail": "task not found"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/library/{resource_id}/versions": {
   "get": {
    "tags": [
     "library"
    ],
    "summary": "List Library Versions",
    "description": "List all versions of a library definition, oldest first.",
    "operationId": "list_library_versions_api_v1_library__resource_id__versions_get",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "resource_id",
      "in": "path",
      "required": true,
      "schema": {
       "type": "string",
       "format": "uuid",
       "title": "Resource Id"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "type": "array",
         "items": {
          "$ref": "#/components/schemas/LibraryVersion"
         },
         "title": "Response List Library Versions Api V1 Library  Resource Id  Versions Get"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "404": {
      "description": "No such resource. Unknown ids return 404; access to an existing but unauthorized transfer returns 403 instead, never 404.",
      "content": {
       "application/json": {
        "example": {
         "detail": "task not found"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   },
   "post": {
    "tags": [
     "library"
    ],
    "summary": "Create Library Version",
    "description": "Add draft version N+1 to a library definition. Never moves `active_version` \u2014 activation is a separate explicit call. Accepts `Idempotency-Key` for safe retries.",
    "operationId": "create_library_version_api_v1_library__resource_id__versions_post",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "resource_id",
      "in": "path",
      "required": true,
      "schema": {
       "type": "string",
       "format": "uuid",
       "title": "Resource Id"
      }
     },
     {
      "name": "Idempotency-Key",
      "in": "header",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string"
        },
        {
         "type": "null"
        }
       ],
       "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`.",
       "title": "Idempotency-Key"
      },
      "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`."
     }
    ],
    "requestBody": {
     "required": true,
     "content": {
      "application/json": {
       "schema": {
        "$ref": "#/components/schemas/LibraryVersionCreate"
       }
      }
     }
    },
    "responses": {
     "201": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/LibraryVersion"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "404": {
      "description": "Library reference not found: unknown id, or no visible resource matches a dependency pin (`pin_not_found`), or the pinned version was never created (`pin_version_not_found` / `version_not_found`). Pins on another user's private resources answer the same 404, never a hint of their existence.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "pin_not_found",
          "stable_key": "..."
         }
        }
       }
      }
     },
     "409": {
      "description": "Replay key problem, no duplicate was created: either the same `Idempotency-Key` was reused with a different body (`idempotency_key_payload_mismatch` \u2014 resend the exact original body) or a previous creation with this key is still completing (`idempotency_key_in_progress` \u2014 retry identically after a short delay). Library reference conflict: the `stable_key` is already taken in this scope (`duplicate_stable_key`); a dependency pin names several visible resources (`pin_ambiguous` \u2014 disambiguate the pin); or a lock already exists for this `(project, resource)` pair (`already_locked`).",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "idempotency_key_payload_mismatch"
         }
        }
       }
      }
     },
     "422": {
      "description": "Library semantic content rejected, nothing stored (P3 semantic content): the version `content` must match its per-kind schema (`content_schema: studio.library.<kind>/v1`, unknown fields forbidden, `rule`/`skill` prose bounded to 65_536 characters, `model_profile` carrying vendor-neutral `requirements` only, `agent_definition` descriptive only, `workflow` declarative only with `participants`, `inputs`/`outputs` and no runtime field). Per-field `details` describe the caller's own payload only. Workflow definition rejected, nothing stored (P11 declarative workflow): the version `content` is schema-valid but structurally incoherent \u2014 duplicate `participant_id` (`duplicate_participant`), a `depends_on` naming no participant of this workflow (`unknown_dependency`), a dependency cycle (`dependency_cycle`), a participant `agent_stable_key` with no matching `composes_agent` pin (`unknown_participant_agent`), a `composes_agent` pin no participant uses (`unused_agent_dependency`), a duplicated I/O name (`duplicate_io_declaration`), or a dataflow `source` that does not resolve to a declared participant output / workflow input (`invalid_io_reference`). Static validation only \u2014 it decides no execution, schedules nothing and stores no run state; raised after the auth/scope gates and before the 404/409 pin gates, never an oracle on invisible resources. Library binding rejected, nothing stored (P5 typed bindings): the dependency pin names a forbidden kind couple (`forbidden_kind_pair`), an explicit relation that does not match the couple (`relation_mismatch`), a second model profile on one agent definition (`too_many_model_profiles`), twice the same target (`duplicate_binding`), or a private user target from a shared definition (`forbidden_scope`). Raised only after the 404/409 existence gates \u2014 never an oracle on invisible resources.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "invalid_content",
          "details": [
           {
            "field": "text",
            "reason": "String should have at least 1 character"
           }
          ]
         }
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/library/{resource_id}/activate": {
   "post": {
    "tags": [
     "library"
    ],
    "summary": "Activate Library Version",
    "description": "Explicitly move `active_version` (sets status to active). Requires the current resource `version`; a stale value is rejected with the live server version. Reactivating the active version is a successful no-op. Accepts `Idempotency-Key` for safe retries.",
    "operationId": "activate_library_version_api_v1_library__resource_id__activate_post",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "resource_id",
      "in": "path",
      "required": true,
      "schema": {
       "type": "string",
       "format": "uuid",
       "title": "Resource Id"
      }
     },
     {
      "name": "Idempotency-Key",
      "in": "header",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string"
        },
        {
         "type": "null"
        }
       ],
       "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`.",
       "title": "Idempotency-Key"
      },
      "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`."
     }
    ],
    "requestBody": {
     "required": true,
     "content": {
      "application/json": {
       "schema": {
        "$ref": "#/components/schemas/LibraryActivate"
       }
      }
     }
    },
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/LibraryResource"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "404": {
      "description": "Library reference not found: unknown id, or no visible resource matches a dependency pin (`pin_not_found`), or the pinned version was never created (`pin_version_not_found` / `version_not_found`). Pins on another user's private resources answer the same 404, never a hint of their existence.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "pin_not_found",
          "stable_key": "..."
         }
        }
       }
      }
     },
     "409": {
      "description": "Stale `If-Match-Version`: another writer changed the object first. `server_version` is the current version \u2014 re-read the object, merge, and retry with the new version. Replay key problem, no duplicate was created: either the same `Idempotency-Key` was reused with a different body (`idempotency_key_payload_mismatch` \u2014 resend the exact original body) or a previous creation with this key is still completing (`idempotency_key_in_progress` \u2014 retry identically after a short delay).",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "version_conflict",
          "server_version": 3
         }
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/library/{resource_id}/deprecate": {
   "post": {
    "tags": [
     "library"
    ],
    "summary": "Deprecate Library Resource",
    "description": "Deprecate a library definition (replaces deletion: history and `active_version` are kept). Requires the current resource `version`. Already deprecated is a successful no-op. Accepts `Idempotency-Key` for safe retries of an unseen response.",
    "operationId": "deprecate_library_resource_api_v1_library__resource_id__deprecate_post",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "resource_id",
      "in": "path",
      "required": true,
      "schema": {
       "type": "string",
       "format": "uuid",
       "title": "Resource Id"
      }
     },
     {
      "name": "Idempotency-Key",
      "in": "header",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string"
        },
        {
         "type": "null"
        }
       ],
       "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`.",
       "title": "Idempotency-Key"
      },
      "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`."
     }
    ],
    "requestBody": {
     "required": true,
     "content": {
      "application/json": {
       "schema": {
        "$ref": "#/components/schemas/LibraryDeprecate"
       }
      }
     }
    },
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/LibraryResource"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "404": {
      "description": "No such resource. Unknown ids return 404; access to an existing but unauthorized transfer returns 403 instead, never 404.",
      "content": {
       "application/json": {
        "example": {
         "detail": "task not found"
        }
       }
      }
     },
     "409": {
      "description": "Stale `If-Match-Version`: another writer changed the object first. `server_version` is the current version \u2014 re-read the object, merge, and retry with the new version. Replay key problem, no duplicate was created: either the same `Idempotency-Key` was reused with a different body (`idempotency_key_payload_mismatch` \u2014 resend the exact original body) or a previous creation with this key is still completing (`idempotency_key_in_progress` \u2014 retry identically after a short delay).",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "version_conflict",
          "server_version": 3
         }
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/library-locks": {
   "get": {
    "tags": [
     "library"
    ],
    "summary": "List Library Locks",
    "description": "List project version locks, optionally filtered by project. Read-only for every authenticated machine, including `readonly`; locks on another user's private resources are filtered out.",
    "operationId": "list_library_locks_api_v1_library_locks_get",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "project_id",
      "in": "query",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string",
         "format": "uuid"
        },
        {
         "type": "null"
        }
       ],
       "title": "Project Id"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "type": "array",
         "items": {
          "$ref": "#/components/schemas/LibraryProjectLock"
         },
         "title": "Response List Library Locks Api V1 Library Locks Get"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   },
   "post": {
    "tags": [
     "library"
    ],
    "summary": "Set Library Lock",
    "description": "Pin `(resource, locked_version)` for a project (canonical resource UUID, never `stable_key` alone). Accepts `Idempotency-Key` for safe retries.",
    "operationId": "set_library_lock_api_v1_library_locks_post",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "Idempotency-Key",
      "in": "header",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string"
        },
        {
         "type": "null"
        }
       ],
       "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`.",
       "title": "Idempotency-Key"
      },
      "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`."
     }
    ],
    "requestBody": {
     "required": true,
     "content": {
      "application/json": {
       "schema": {
        "$ref": "#/components/schemas/LibraryLockCreate"
       }
      }
     }
    },
    "responses": {
     "201": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/LibraryProjectLock"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "404": {
      "description": "No such resource. Unknown ids return 404; access to an existing but unauthorized transfer returns 403 instead, never 404.",
      "content": {
       "application/json": {
        "example": {
         "detail": "task not found"
        }
       }
      }
     },
     "409": {
      "description": "Replay key problem, no duplicate was created: either the same `Idempotency-Key` was reused with a different body (`idempotency_key_payload_mismatch` \u2014 resend the exact original body) or a previous creation with this key is still completing (`idempotency_key_in_progress` \u2014 retry identically after a short delay). Library reference conflict: the `stable_key` is already taken in this scope (`duplicate_stable_key`); a dependency pin names several visible resources (`pin_ambiguous` \u2014 disambiguate the pin); or a lock already exists for this `(project, resource)` pair (`already_locked`).",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "idempotency_key_payload_mismatch"
         }
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/library-locks/{lock_id}": {
   "delete": {
    "tags": [
     "library"
    ],
    "summary": "Release Library Lock",
    "description": "Release a project lock. Only the creating user (or an admin) may release it.",
    "operationId": "release_library_lock_api_v1_library_locks__lock_id__delete",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "lock_id",
      "in": "path",
      "required": true,
      "schema": {
       "type": "string",
       "format": "uuid",
       "title": "Lock Id"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/LibraryProjectLock"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "404": {
      "description": "No such resource. Unknown ids return 404; access to an existing but unauthorized transfer returns 403 instead, never 404.",
      "content": {
       "application/json": {
        "example": {
         "detail": "task not found"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/runtime-bindings": {
   "get": {
    "tags": [
     "runtime-bindings"
    ],
    "summary": "List Runtime Bindings",
    "description": "List stored runtime choices, optionally filtered. Another user's `user`-level bindings are filtered out before exposure \u2014 collections never count, list, or hint at them.",
    "operationId": "list_runtime_bindings_api_v1_runtime_bindings_get",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "level",
      "in": "query",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "$ref": "#/components/schemas/RuntimeLevel"
        },
        {
         "type": "null"
        }
       ],
       "title": "Level"
      }
     },
     {
      "name": "project_id",
      "in": "query",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string",
         "format": "uuid"
        },
        {
         "type": "null"
        }
       ],
       "title": "Project Id"
      }
     },
     {
      "name": "kind",
      "in": "query",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "$ref": "#/components/schemas/LibraryKind"
        },
        {
         "type": "null"
        }
       ],
       "title": "Kind"
      }
     },
     {
      "name": "stable_key",
      "in": "query",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string"
        },
        {
         "type": "null"
        }
       ],
       "title": "Stable Key"
      }
     },
     {
      "name": "limit",
      "in": "query",
      "required": false,
      "schema": {
       "type": "integer",
       "maximum": 500,
       "default": 100,
       "title": "Limit"
      }
     },
     {
      "name": "offset",
      "in": "query",
      "required": false,
      "schema": {
       "type": "integer",
       "default": 0,
       "title": "Offset"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "type": "array",
         "items": {
          "$ref": "#/components/schemas/RuntimeBinding"
         },
         "title": "Response List Runtime Bindings Api V1 Runtime Bindings Get"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   },
   "post": {
    "tags": [
     "runtime-bindings"
    ],
    "summary": "Create Runtime Binding",
    "description": "Store one runtime choice for a logical `(kind, stable_key)` key. The owner is always the caller (server-derived). Only the four stored levels persist \u2014 `session` is ephemeral and rejected here (`ephemeral_level_not_stored`); pass it to the resolution call instead. Accepts `Idempotency-Key` for safe retries.",
    "operationId": "create_runtime_binding_api_v1_runtime_bindings_post",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "Idempotency-Key",
      "in": "header",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string"
        },
        {
         "type": "null"
        }
       ],
       "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`.",
       "title": "Idempotency-Key"
      },
      "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`."
     }
    ],
    "requestBody": {
     "required": true,
     "content": {
      "application/json": {
       "schema": {
        "$ref": "#/components/schemas/RuntimeBindingCreate"
       }
      }
     }
    },
    "responses": {
     "201": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/RuntimeBinding"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "404": {
      "description": "Runtime reference not found: unknown runtime id (`runtime not found`, plain message \u2014 another user's runtime answers the same 404, never a hint of its existence), unknown runtime binding id (`runtime binding not found`), a `runtime_id` reference naming no row (`runtime_not_found`), an attached/bound machine that does not exist (`runtime_target_not_found`), or a project context that does not exist (`project_not_found`).",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "runtime_not_found"
         }
        }
       }
      }
     },
     "409": {
      "description": "Replay key problem, no duplicate was created: either the same `Idempotency-Key` was reused with a different body (`idempotency_key_payload_mismatch` \u2014 resend the exact original body) or a previous creation with this key is still completing (`idempotency_key_in_progress` \u2014 retry identically after a short delay). Runtime choice already stored for this `(level, scope, kind, stable_key)` key (`already_bound`) \u2014 release it first, then store the new choice. Bindings are set-once per key, never silently overwritten.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "idempotency_key_payload_mismatch"
         }
        }
       }
      }
     },
     "422": {
      "description": "Runtime choice rejected, nothing stored: a `session` level sent for persistence (`ephemeral_level_not_stored`), a kind outside `agent_definition`/`model_profile` (`unsupported_target_kind`), a `runtime_id` mixed with inline anchors (`runtime_id_must_be_exclusive`), or an update/register leaving no anchor at all (`invalid_runtime` / `no_anchor_left`). Secret-looking metadata keys are rejected by validation before anything is stored.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "invalid_runtime_binding",
          "reason": "ephemeral_level_not_stored"
         }
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/runtime-bindings/{binding_id}": {
   "get": {
    "tags": [
     "runtime-bindings"
    ],
    "summary": "Get Runtime Binding",
    "description": "Get one stored runtime choice. Another user's `user`-level binding answers 404, never 403, so its existence cannot be inferred.",
    "operationId": "get_runtime_binding_api_v1_runtime_bindings__binding_id__get",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "binding_id",
      "in": "path",
      "required": true,
      "schema": {
       "type": "string",
       "format": "uuid",
       "title": "Binding Id"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/RuntimeBinding"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "404": {
      "description": "No such resource. Unknown ids return 404; access to an existing but unauthorized transfer returns 403 instead, never 404.",
      "content": {
       "application/json": {
        "example": {
         "detail": "task not found"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   },
   "delete": {
    "tags": [
     "runtime-bindings"
    ],
    "summary": "Delete Runtime Binding",
    "description": "Release a stored runtime choice (snapshot returned). Release rules mirror project locks: `user` level by owner-or-admin, shared project levels by creator-or-admin, `studio_default` by privileged roles. Naturally idempotent \u2014 no `Idempotency-Key` needed.",
    "operationId": "delete_runtime_binding_api_v1_runtime_bindings__binding_id__delete",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "binding_id",
      "in": "path",
      "required": true,
      "schema": {
       "type": "string",
       "format": "uuid",
       "title": "Binding Id"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/RuntimeBinding"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "404": {
      "description": "No such resource. Unknown ids return 404; access to an existing but unauthorized transfer returns 403 instead, never 404.",
      "content": {
       "application/json": {
        "example": {
         "detail": "task not found"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/runtimes": {
   "get": {
    "tags": [
     "runtimes"
    ],
    "summary": "List Runtimes",
    "description": "List runtimes declared by the caller. Runtimes are always per-user private: another user's rows are filtered out before exposure. Revoked rows are excluded by default.",
    "operationId": "list_runtimes_api_v1_runtimes_get",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "status",
      "in": "query",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "$ref": "#/components/schemas/RuntimeStatus"
        },
        {
         "type": "null"
        }
       ],
       "title": "Status"
      }
     },
     {
      "name": "include_revoked",
      "in": "query",
      "required": false,
      "schema": {
       "type": "boolean",
       "default": false,
       "title": "Include Revoked"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "type": "array",
         "items": {
          "$ref": "#/components/schemas/RuntimeRegistration"
         },
         "title": "Response List Runtimes Api V1 Runtimes Get"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   },
   "post": {
    "tags": [
     "runtimes"
    ],
    "summary": "Register Runtime",
    "description": "Declare one runtime (generic \u2014 never provider-specific). The owner is always the caller (server-derived); an attached machine must exist and be owned by the caller. Refs are open chains, never a vendor catalog. Secret-looking metadata keys are rejected. Accepts `Idempotency-Key` for safe retries.",
    "operationId": "register_runtime_api_v1_runtimes_post",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "Idempotency-Key",
      "in": "header",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string"
        },
        {
         "type": "null"
        }
       ],
       "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`.",
       "title": "Idempotency-Key"
      },
      "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`."
     }
    ],
    "requestBody": {
     "required": true,
     "content": {
      "application/json": {
       "schema": {
        "$ref": "#/components/schemas/RuntimeRegistrationCreate"
       }
      }
     }
    },
    "responses": {
     "201": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/RuntimeRegistration"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "404": {
      "description": "Runtime reference not found: unknown runtime id (`runtime not found`, plain message \u2014 another user's runtime answers the same 404, never a hint of its existence), unknown runtime binding id (`runtime binding not found`), a `runtime_id` reference naming no row (`runtime_not_found`), an attached/bound machine that does not exist (`runtime_target_not_found`), or a project context that does not exist (`project_not_found`).",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "runtime_not_found"
         }
        }
       }
      }
     },
     "409": {
      "description": "Replay key problem, no duplicate was created: either the same `Idempotency-Key` was reused with a different body (`idempotency_key_payload_mismatch` \u2014 resend the exact original body) or a previous creation with this key is still completing (`idempotency_key_in_progress` \u2014 retry identically after a short delay).",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "idempotency_key_payload_mismatch"
         }
        }
       }
      }
     },
     "422": {
      "description": "Runtime choice rejected, nothing stored: a `session` level sent for persistence (`ephemeral_level_not_stored`), a kind outside `agent_definition`/`model_profile` (`unsupported_target_kind`), a `runtime_id` mixed with inline anchors (`runtime_id_must_be_exclusive`), or an update/register leaving no anchor at all (`invalid_runtime` / `no_anchor_left`). Secret-looking metadata keys are rejected by validation before anything is stored.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "invalid_runtime_binding",
          "reason": "ephemeral_level_not_stored"
         }
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/runtimes/{runtime_id}": {
   "get": {
    "tags": [
     "runtimes"
    ],
    "summary": "Get Runtime",
    "description": "Get one declared runtime. Another user's runtime answers 404, never 403, so its existence cannot be inferred. Revoked rows stay readable (diagnostics).",
    "operationId": "get_runtime_api_v1_runtimes__runtime_id__get",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "runtime_id",
      "in": "path",
      "required": true,
      "schema": {
       "type": "string",
       "format": "uuid",
       "title": "Runtime Id"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/RuntimeRegistration"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "404": {
      "description": "No such resource. Unknown ids return 404; access to an existing but unauthorized transfer returns 403 instead, never 404.",
      "content": {
       "application/json": {
        "example": {
         "detail": "task not found"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   },
   "patch": {
    "tags": [
     "runtimes"
    ],
    "summary": "Update Runtime",
    "description": "Mutate a runtime's descriptors without rotating its identity. Requires the current `version`; a stale value is rejected with the live server version. Accepts `Idempotency-Key` for safe retries of an unseen response.",
    "operationId": "update_runtime_api_v1_runtimes__runtime_id__patch",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "runtime_id",
      "in": "path",
      "required": true,
      "schema": {
       "type": "string",
       "format": "uuid",
       "title": "Runtime Id"
      }
     },
     {
      "name": "Idempotency-Key",
      "in": "header",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string"
        },
        {
         "type": "null"
        }
       ],
       "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`.",
       "title": "Idempotency-Key"
      },
      "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`."
     }
    ],
    "requestBody": {
     "required": true,
     "content": {
      "application/json": {
       "schema": {
        "$ref": "#/components/schemas/RuntimeRegistrationUpdateRequest"
       }
      }
     }
    },
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/RuntimeRegistration"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "404": {
      "description": "No such resource. Unknown ids return 404; access to an existing but unauthorized transfer returns 403 instead, never 404.",
      "content": {
       "application/json": {
        "example": {
         "detail": "task not found"
        }
       }
      }
     },
     "409": {
      "description": "Replay key problem, no duplicate was created: either the same `Idempotency-Key` was reused with a different body (`idempotency_key_payload_mismatch` \u2014 resend the exact original body) or a previous creation with this key is still completing (`idempotency_key_in_progress` \u2014 retry identically after a short delay). Stale `If-Match-Version`: another writer changed the object first. `server_version` is the current version \u2014 re-read the object, merge, and retry with the new version.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "idempotency_key_payload_mismatch"
         }
        }
       }
      }
     },
     "422": {
      "description": "Runtime choice rejected, nothing stored: a `session` level sent for persistence (`ephemeral_level_not_stored`), a kind outside `agent_definition`/`model_profile` (`unsupported_target_kind`), a `runtime_id` mixed with inline anchors (`runtime_id_must_be_exclusive`), or an update/register leaving no anchor at all (`invalid_runtime` / `no_anchor_left`). Secret-looking metadata keys are rejected by validation before anything is stored.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "invalid_runtime_binding",
          "reason": "ephemeral_level_not_stored"
         }
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/runtimes/{runtime_id}/revoke": {
   "post": {
    "tags": [
     "runtimes"
    ],
    "summary": "Revoke Runtime",
    "description": "Logically revoke a runtime (idempotent: already revoked is a successful no-op). The row stays readable but resolves as non-live, so bindings toward it fall through instead of silently retargeting. No physical delete exists. Naturally idempotent \u2014 no `Idempotency-Key` needed.",
    "operationId": "revoke_runtime_api_v1_runtimes__runtime_id__revoke_post",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "runtime_id",
      "in": "path",
      "required": true,
      "schema": {
       "type": "string",
       "format": "uuid",
       "title": "Runtime Id"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/RuntimeRegistration"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "404": {
      "description": "No such resource. Unknown ids return 404; access to an existing but unauthorized transfer returns 403 instead, never 404.",
      "content": {
       "application/json": {
        "example": {
         "detail": "task not found"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/resolutions": {
   "post": {
    "tags": [
     "resolutions"
    ],
    "summary": "Resolve Agent Definition",
    "description": "Resolve an `AgentDefinition` to its canonical `ResolvedAgentDefinition`: effective definition version, rules, skills, model profile, requirements, winning runtime, compatibility verdict and full structured provenance. Delegates to `resolve_full` \u2014 this route never re-decides anything (no fallback to another runtime on incompatibility). Optional `session_overrides` are ephemeral resolution context: validated like stored choices (unknown machine 404, another user's machine or runtime 403), winning per P4 precedence, appearing in provenance \u2014 and never persisted. Overrides naming a key outside the resolved set (agent definition + linked model profile) are ignored. Pure read: safe to retry, no `Idempotency-Key` needed.",
    "operationId": "resolve_agent_definition_api_v1_resolutions_post",
    "requestBody": {
     "content": {
      "application/json": {
       "schema": {
        "$ref": "#/components/schemas/AgentResolutionRequest"
       }
      }
     },
     "required": true
    },
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/ResolvedAgentDefinition"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "404": {
      "description": "Runtime reference not found: unknown runtime id (`runtime not found`, plain message \u2014 another user's runtime answers the same 404, never a hint of its existence), unknown runtime binding id (`runtime binding not found`), a `runtime_id` reference naming no row (`runtime_not_found`), an attached/bound machine that does not exist (`runtime_target_not_found`), or a project context that does not exist (`project_not_found`).",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "runtime_not_found"
         }
        }
       }
      }
     },
     "422": {
      "description": "Resolution refused, nothing stored (pure read): the winning runtime choice does not satisfy the linked model profile requirements (`runtime_incompatible`, with `level`, `matched_kind`, `matched_stable_key` and `unsatisfied` \u2014 never a silent fallback to another runtime), or the loaded snapshot is internally inconsistent (`invalid_resolution_input`, including a duplicated session override key). Missing or invisible definitions answer 404 `definition_not_found` instead, never a hint of their existence.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "runtime_incompatible",
          "level": "user",
          "matched_kind": "agent_definition",
          "matched_stable_key": "...",
          "unsatisfied": [
           "coding: required"
          ]
         }
        }
       }
      }
     }
    },
    "security": [
     {
      "MachineBearer": []
     }
    ]
   }
  },
  "/api/v1/agents": {
   "get": {
    "tags": [
     "agents"
    ],
    "summary": "List Agents",
    "description": "List agent provenance identities. Any authenticated machine may read.",
    "operationId": "list_agents_api_v1_agents_get",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "type": "array",
         "items": {
          "$ref": "#/components/schemas/Agent"
         },
         "title": "Response List Agents Api V1 Agents Get"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     }
    }
   },
   "post": {
    "tags": [
     "agents"
    ],
    "summary": "Register Agent",
    "description": "Register an agent identity for the caller's own authenticated machine. `machine_id` is always derived from the credential \u2014 never send it. `display_name` is required; `agent_kind`, `agent_profile`, `harness`, `provider` and `model` are optional free-form metadata (open strings, default null, every value accepted). Registration confers no permission and is required for nothing except attributing AI work logs; authentication and authorization work without it. Accepts `Idempotency-Key` for safe retries.",
    "operationId": "register_agent_api_v1_agents_post",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "Idempotency-Key",
      "in": "header",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string"
        },
        {
         "type": "null"
        }
       ],
       "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`.",
       "title": "Idempotency-Key"
      },
      "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`."
     }
    ],
    "requestBody": {
     "required": true,
     "content": {
      "application/json": {
       "schema": {
        "$ref": "#/components/schemas/AgentCreate"
       }
      }
     }
    },
    "responses": {
     "201": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/Agent"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "409": {
      "description": "Replay key problem, no duplicate was created: either the same `Idempotency-Key` was reused with a different body (`idempotency_key_payload_mismatch` \u2014 resend the exact original body) or a previous creation with this key is still completing (`idempotency_key_in_progress` \u2014 retry identically after a short delay).",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "idempotency_key_payload_mismatch"
         }
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/ai-work": {
   "get": {
    "tags": [
     "ai-work"
    ],
    "summary": "List Ai Work",
    "description": "List AI work ledger entries, optionally filtered by project or task. Any authenticated machine may read.",
    "operationId": "list_ai_work_api_v1_ai_work_get",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "project_id",
      "in": "query",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string",
         "format": "uuid"
        },
        {
         "type": "null"
        }
       ],
       "title": "Project Id"
      }
     },
     {
      "name": "task_id",
      "in": "query",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string",
         "format": "uuid"
        },
        {
         "type": "null"
        }
       ],
       "title": "Task Id"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "type": "array",
         "items": {
          "$ref": "#/components/schemas/AIWorkLog"
         },
         "title": "Response List Ai Work Api V1 Ai Work Get"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   },
   "post": {
    "tags": [
     "ai-work"
    ],
    "summary": "Create Ai Work",
    "description": "Log a unit of AI work. Requires a writer role. `agent_id` must reference an agent attached to the caller's own authenticated machine (register one with `POST /agents` first) \u2014 a foreign or unknown agent fails with `409 actor_not_owned`, never a silent cross-machine attribution. `agent_profile`, `harness`, `provider` and `model` are optional open-string observability metadata: any value is accepted, none is required, none affects authorization. Accepts `Idempotency-Key` for safe retries.",
    "operationId": "create_ai_work_api_v1_ai_work_post",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "Idempotency-Key",
      "in": "header",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string"
        },
        {
         "type": "null"
        }
       ],
       "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`.",
       "title": "Idempotency-Key"
      },
      "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`."
     }
    ],
    "requestBody": {
     "required": true,
     "content": {
      "application/json": {
       "schema": {
        "$ref": "#/components/schemas/AIWorkLogCreate"
       }
      }
     }
    },
    "responses": {
     "201": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/AIWorkLog"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "409": {
      "description": "Provenance mismatch: the referenced agent identity is unknown or attached to another machine. Register an agent for the caller's own authenticated machine first (`POST /agents`), then reference it. Never silently attributed across machines. Replay key problem, no duplicate was created: either the same `Idempotency-Key` was reused with a different body (`idempotency_key_payload_mismatch` \u2014 resend the exact original body) or a previous creation with this key is still completing (`idempotency_key_in_progress` \u2014 retry identically after a short delay).",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "actor_not_owned",
          "message": "agent_id must be an agent attached to the authenticated machine"
         }
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/ai-work/{work_id}": {
   "patch": {
    "tags": [
     "ai-work"
    ],
    "summary": "Update Ai Work",
    "description": "Update your own work entry (status, changed files, tests run). Only the owning machine's entries may be updated. Resolving a review (`approved` or changes requested) additionally requires a privileged role and the `review_requested` state \u2014 anything else fails with `409 invalid_status_transition`.",
    "operationId": "update_ai_work_api_v1_ai_work__work_id__patch",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "work_id",
      "in": "path",
      "required": true,
      "schema": {
       "type": "string",
       "format": "uuid",
       "title": "Work Id"
      }
     }
    ],
    "requestBody": {
     "required": true,
     "content": {
      "application/json": {
       "schema": {
        "$ref": "#/components/schemas/AIWorkLogUpdate"
       }
      }
     }
    },
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/AIWorkLog"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "404": {
      "description": "No such resource. Unknown ids return 404; access to an existing but unauthorized transfer returns 403 instead, never 404.",
      "content": {
       "application/json": {
        "example": {
         "detail": "task not found"
        }
       }
      }
     },
     "409": {
      "description": "Invalid review transition: resolving a work entry to `approved` (or requesting changes) requires a privileged role and is only possible from `review_requested` \u2014 nobody approves their own work. Move the entry to `review_requested` first, then have a privileged reviewer resolve it.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "invalid_status_transition"
         }
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/review-queue": {
   "get": {
    "tags": [
     "review-queue"
    ],
    "summary": "Get Review Queue",
    "description": "Aggregated view of everything waiting on a human decision: AI work in `review_requested` (resolve via `PATCH /ai-work/{id}`), decisions still `proposed` (informational \u2014 no transition endpoint exists for decisions), recent `resource.conflict` events within `conflict_window_hours` (best-effort and time-windowed: no persisted conflict state exists, an old unaddressed conflict silently ages out of the window), failed builds (`build_failure`, informational \u2014 no build transition endpoint exists), and opened PRs with no merge yet (`pr_ready`, best-effort and time-windowed like conflicts). Also serves as the notifications surface \u2014 there is no separate notifications endpoint. Any authenticated machine may read. Clients must tolerate an unknown `kind`.",
    "operationId": "get_review_queue_api_v1_review_queue_get",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "project_id",
      "in": "query",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string",
         "format": "uuid"
        },
        {
         "type": "null"
        }
       ],
       "title": "Project Id"
      }
     },
     {
      "name": "conflict_window_hours",
      "in": "query",
      "required": false,
      "schema": {
       "type": "integer",
       "maximum": 168,
       "default": 24,
       "title": "Conflict Window Hours"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/ReviewQueue"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/timeline": {
   "get": {
    "tags": [
     "timeline"
    ],
    "summary": "Get Timeline",
    "description": "Day-grouped project activity (newest day first), unfiltered \u2014 the full history, not an actionable signal (see GET /review-queue for that). Inherits GET /events's 'not claimed exhaustive' honesty: several event types have no server-side emission yet. Any authenticated machine may read.",
    "operationId": "get_timeline_api_v1_timeline_get",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "project_id",
      "in": "query",
      "required": true,
      "schema": {
       "type": "string",
       "format": "uuid",
       "title": "Project Id"
      }
     },
     {
      "name": "since",
      "in": "query",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string",
         "format": "date-time"
        },
        {
         "type": "null"
        }
       ],
       "title": "Since"
      }
     },
     {
      "name": "limit",
      "in": "query",
      "required": false,
      "schema": {
       "type": "integer",
       "maximum": 500,
       "default": 200,
       "title": "Limit"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/Timeline"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/github/webhook": {
   "post": {
    "tags": [
     "github"
    ],
    "summary": "Github Webhook",
    "description": "GitHub webhook ingress. Authenticated by `X-Hub-Signature-256` HMAC over the raw body with the process-wide webhook secret \u2014 never a machine Bearer token, so this is a deliberate signed authentication exception. Unknown event names and unconfigured repositories answer `202` (accepted-but-ignored), never `500`.",
    "operationId": "github_webhook_api_v1_github_webhook_post",
    "parameters": [
     {
      "name": "X-GitHub-Event",
      "in": "header",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string"
        },
        {
         "type": "null"
        }
       ],
       "title": "X-Github-Event"
      }
     },
     {
      "name": "X-GitHub-Delivery",
      "in": "header",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string"
        },
        {
         "type": "null"
        }
       ],
       "title": "X-Github-Delivery"
      }
     },
     {
      "name": "X-Hub-Signature-256",
      "in": "header",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string"
        },
        {
         "type": "null"
        }
       ],
       "title": "X-Hub-Signature-256"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/GitHubWebhookResult"
        }
       }
      }
     },
     "401": {
      "description": "Invalid or missing GitHub webhook signature. The `X-Hub-Signature-256` HMAC over the raw body did not verify \u2014 deliberately a plain message with no machine-readable code, so a prober learns nothing. Retry with the correct `STUDIO_GITHUB_WEBHOOK_SECRET`.",
      "content": {
       "application/json": {
        "example": {
         "detail": "invalid webhook signature"
        }
       }
      }
     },
     "503": {
      "description": "GitHub webhook ingress is not configured on this server (`STUDIO_GITHUB_WEBHOOK_SECRET` unset).",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "webhook_not_configured"
         }
        }
       }
      }
     },
     "413": {
      "description": "Webhook body exceeds the bounded ingress size (`STUDIO_GITHUB_WEBHOOK_MAX_BODY_BYTES`).",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "webhook_body_too_large"
         }
        }
       }
      }
     },
     "400": {
      "description": "Webhook delivery malformed: missing `X-GitHub-Event`/`X-GitHub-Delivery` headers (`webhook_missing_headers`) or unparsable JSON (`webhook_invalid_json`).",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "webhook_missing_headers"
         }
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/projects/{project_id}/github-integration": {
   "post": {
    "tags": [
     "github"
    ],
    "summary": "Create Github Integration",
    "description": "Wire a project to a GitHub repository. Role `admin` or `developer`. Accepts `Idempotency-Key` for safe retries.",
    "operationId": "create_github_integration_api_v1_projects__project_id__github_integration_post",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "project_id",
      "in": "path",
      "required": true,
      "schema": {
       "type": "string",
       "format": "uuid",
       "title": "Project Id"
      }
     },
     {
      "name": "Idempotency-Key",
      "in": "header",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string"
        },
        {
         "type": "null"
        }
       ],
       "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`.",
       "title": "Idempotency-Key"
      },
      "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`."
     }
    ],
    "requestBody": {
     "required": true,
     "content": {
      "application/json": {
       "schema": {
        "$ref": "#/components/schemas/GitHubIntegrationCreate"
       }
      }
     }
    },
    "responses": {
     "201": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/GitHubIntegration"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "409": {
      "description": "Replay key problem, no duplicate was created: either the same `Idempotency-Key` was reused with a different body (`idempotency_key_payload_mismatch` \u2014 resend the exact original body) or a previous creation with this key is still completing (`idempotency_key_in_progress` \u2014 retry identically after a short delay).",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "idempotency_key_payload_mismatch"
         }
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   },
   "get": {
    "tags": [
     "github"
    ],
    "summary": "Get Github Integration",
    "description": "Read a project's GitHub wiring. Any authenticated machine may read.",
    "operationId": "get_github_integration_api_v1_projects__project_id__github_integration_get",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "project_id",
      "in": "path",
      "required": true,
      "schema": {
       "type": "string",
       "format": "uuid",
       "title": "Project Id"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/GitHubIntegration"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "404": {
      "description": "No such resource. Unknown ids return 404; access to an existing but unauthorized transfer returns 403 instead, never 404.",
      "content": {
       "application/json": {
        "example": {
         "detail": "task not found"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   },
   "patch": {
    "tags": [
     "github"
    ],
    "summary": "Update Github Integration",
    "description": "Update a project's GitHub wiring (repository, default branch, enabled flag). Role `admin` or `developer`.",
    "operationId": "update_github_integration_api_v1_projects__project_id__github_integration_patch",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "project_id",
      "in": "path",
      "required": true,
      "schema": {
       "type": "string",
       "format": "uuid",
       "title": "Project Id"
      }
     }
    ],
    "requestBody": {
     "required": true,
     "content": {
      "application/json": {
       "schema": {
        "$ref": "#/components/schemas/GitHubIntegrationUpdate"
       }
      }
     }
    },
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/GitHubIntegration"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "404": {
      "description": "No such resource. Unknown ids return 404; access to an existing but unauthorized transfer returns 403 instead, never 404.",
      "content": {
       "application/json": {
        "example": {
         "detail": "task not found"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/builds": {
   "get": {
    "tags": [
     "builds"
    ],
    "summary": "List Builds",
    "description": "List CI builds observed on wired GitHub repositories, newest first. Any authenticated machine may read.",
    "operationId": "list_builds_api_v1_builds_get",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "project_id",
      "in": "query",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string",
         "format": "uuid"
        },
        {
         "type": "null"
        }
       ],
       "title": "Project Id"
      }
     },
     {
      "name": "status",
      "in": "query",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "$ref": "#/components/schemas/BuildStatus"
        },
        {
         "type": "null"
        }
       ],
       "title": "Status"
      }
     },
     {
      "name": "limit",
      "in": "query",
      "required": false,
      "schema": {
       "type": "integer",
       "maximum": 500,
       "default": 100,
       "title": "Limit"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "type": "array",
         "items": {
          "$ref": "#/components/schemas/Build"
         },
         "title": "Response List Builds Api V1 Builds Get"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/builds/{build_id}": {
   "get": {
    "tags": [
     "builds"
    ],
    "summary": "Get Build",
    "description": "Get one build by id. Any authenticated machine may read.",
    "operationId": "get_build_api_v1_builds__build_id__get",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "build_id",
      "in": "path",
      "required": true,
      "schema": {
       "type": "string",
       "format": "uuid",
       "title": "Build Id"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/Build"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "404": {
      "description": "No such resource. Unknown ids return 404; access to an existing but unauthorized transfer returns 403 instead, never 404.",
      "content": {
       "application/json": {
        "example": {
         "detail": "task not found"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/producer-jobs": {
   "post": {
    "tags": [
     "producer"
    ],
    "summary": "Request Producer Job",
    "description": "Run a bounded, synchronous Studio Producer analysis over one project's shared state. Requires a writer role. Accepts `Idempotency-Key` for safe retries. The Producer never mutates tasks or claims: a `decomposition` result is a proposal the caller materializes via `POST /tasks`.",
    "operationId": "request_producer_job_api_v1_producer_jobs_post",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "Idempotency-Key",
      "in": "header",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string"
        },
        {
         "type": "null"
        }
       ],
       "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`.",
       "title": "Idempotency-Key"
      },
      "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`."
     }
    ],
    "requestBody": {
     "required": true,
     "content": {
      "application/json": {
       "schema": {
        "$ref": "#/components/schemas/ProducerJobRequest"
       }
      }
     }
    },
    "responses": {
     "201": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/ProducerJob"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "409": {
      "description": "Replay key problem, no duplicate was created: either the same `Idempotency-Key` was reused with a different body (`idempotency_key_payload_mismatch` \u2014 resend the exact original body) or a previous creation with this key is still completing (`idempotency_key_in_progress` \u2014 retry identically after a short delay).",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "idempotency_key_payload_mismatch"
         }
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   },
   "get": {
    "tags": [
     "producer"
    ],
    "summary": "List Producer Jobs",
    "description": "List Producer jobs, newest first. Any authenticated machine may read.",
    "operationId": "list_producer_jobs_api_v1_producer_jobs_get",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "project_id",
      "in": "query",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string",
         "format": "uuid"
        },
        {
         "type": "null"
        }
       ],
       "title": "Project Id"
      }
     },
     {
      "name": "limit",
      "in": "query",
      "required": false,
      "schema": {
       "type": "integer",
       "maximum": 500,
       "default": 100,
       "title": "Limit"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "type": "array",
         "items": {
          "$ref": "#/components/schemas/ProducerJob"
         },
         "title": "Response List Producer Jobs Api V1 Producer Jobs Get"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/producer-jobs/{job_id}": {
   "get": {
    "tags": [
     "producer"
    ],
    "summary": "Get Producer Job",
    "description": "Get one Producer job by id. Any authenticated machine may read.",
    "operationId": "get_producer_job_api_v1_producer_jobs__job_id__get",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "job_id",
      "in": "path",
      "required": true,
      "schema": {
       "type": "string",
       "format": "uuid",
       "title": "Job Id"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/ProducerJob"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "404": {
      "description": "No such resource. Unknown ids return 404; access to an existing but unauthorized transfer returns 403 instead, never 404.",
      "content": {
       "application/json": {
        "example": {
         "detail": "task not found"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/heartbeats": {
   "post": {
    "tags": [
     "heartbeats"
    ],
    "summary": "Post Heartbeat",
    "description": "Report machine presence. Any authenticated machine may heartbeat, including read-only ones \u2014 this is the one write that never requires a writer role. `machine_id` must equal the authenticated machine's own id. `status` is derived server-side from the last report, never stored from the client; send roughly every 30 seconds.",
    "operationId": "post_heartbeat_api_v1_heartbeats_post",
    "requestBody": {
     "content": {
      "application/json": {
       "schema": {
        "$ref": "#/components/schemas/HeartbeatRequest"
       }
      }
     },
     "required": true
    },
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HeartbeatResponse"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "409": {
      "description": "`machine_id` in the body must equal the authenticated machine's own id \u2014 it is never a free field. Send the caller's own id, never another machine's.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "machine_id_mismatch",
          "message": "machine_id does not match the authenticated machine"
         }
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    },
    "security": [
     {
      "MachineBearer": []
     }
    ]
   }
  },
  "/api/v1/events": {
   "post": {
    "tags": [
     "events"
    ],
    "summary": "Post Event",
    "description": "Publish an event to a project. Requires a writer role. `event_id` is a caller-generated UUID and the replay key: resending the same `event_id` returns the stored event, never a duplicate (this endpoint does not use `Idempotency-Key`). Identity is validated against the authenticated machine: omit `machine_id` (it is derived automatically) or send the machine's own id; `actor_type=user` requires the machine owner's user id as `actor_id`; `actor_type=agent` requires an agent attached to the caller's own machine (register one with `POST /agents` first); `actor_type=system` requires the machine's own id. Working without an agent identity is fully supported \u2014 use `user` or `system`.",
    "operationId": "post_event_api_v1_events_post",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "requestBody": {
     "required": true,
     "content": {
      "application/json": {
       "schema": {
        "$ref": "#/components/schemas/EventCreate"
       }
      }
     }
    },
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/EventEnvelope"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "409": {
      "description": "Event identity rejected before anything is stored: `machine_id` must be omitted (it is derived from the authenticated machine) or equal to it (`machine_id_mismatch`); `actor_type=user` requires `actor_id` equal to the machine owner's user id (`actor_id_mismatch`); `actor_type=agent` requires an agent attached to the caller's machine, and `actor_type=system` requires `actor_id` equal to the machine's own id (`actor_not_owned`). Replaying an already-stored `event_id` with a different identity never modifies the original.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "machine_id_mismatch",
          "message": "machine_id does not match the authenticated machine"
         }
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   },
   "get": {
    "tags": [
     "events"
    ],
    "summary": "Get Events",
    "description": "Read recent events, optionally filtered by project, task and `since` timestamp. Any authenticated machine may read. This is the polling and catch-up channel: after a disconnect, poll with `since` to retrieve missed history, then optionally resume live delivery on `GET /events/stream`.",
    "operationId": "get_events_api_v1_events_get",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "project",
      "in": "query",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string",
         "format": "uuid"
        },
        {
         "type": "null"
        }
       ],
       "title": "Project"
      }
     },
     {
      "name": "task",
      "in": "query",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string",
         "format": "uuid"
        },
        {
         "type": "null"
        }
       ],
       "title": "Task"
      }
     },
     {
      "name": "since",
      "in": "query",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string",
         "format": "date-time"
        },
        {
         "type": "null"
        }
       ],
       "title": "Since"
      }
     },
     {
      "name": "limit",
      "in": "query",
      "required": false,
      "schema": {
       "type": "integer",
       "maximum": 500,
       "default": 100,
       "title": "Limit"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "type": "array",
         "items": {
          "$ref": "#/components/schemas/EventEnvelope"
         },
         "title": "Response Get Events Api V1 Events Get"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/events/stream": {
   "get": {
    "tags": [
     "events"
    ],
    "summary": "Stream Events",
    "description": "Live event push for one project as Server-Sent Events (`text/event-stream`; each SSE `id:` is the event's `seq`, a strictly increasing integer). `project` is required \u2014 there is no global cross-project stream. Resuming after a cutover loses nothing: pass the last seen `seq` as `since_seq`, or rely on the standard SSE `Last-Event-ID` auto-reconnect header (`Last-Event-ID` wins when both are present). With no cursor, only events created from connection time are delivered \u2014 use `GET /events?since=` first to backfill history, then open the stream for live updates. Same authentication as the rest of the API.",
    "operationId": "stream_events_api_v1_events_stream_get",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "project",
      "in": "query",
      "required": true,
      "schema": {
       "type": "string",
       "format": "uuid",
       "description": "Project to subscribe to (UUID). No global stream exists.",
       "title": "Project"
      },
      "description": "Project to subscribe to (UUID). No global stream exists."
     },
     {
      "name": "since_seq",
      "in": "query",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "integer"
        },
        {
         "type": "null"
        }
       ],
       "description": "Explicit resume cursor: last `seq` already seen. Ignored when `Last-Event-ID` is present.",
       "title": "Since Seq"
      },
      "description": "Explicit resume cursor: last `seq` already seen. Ignored when `Last-Event-ID` is present."
     },
     {
      "name": "Last-Event-ID",
      "in": "header",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string"
        },
        {
         "type": "null"
        }
       ],
       "description": "Standard SSE auto-reconnect cursor (integer `seq`). Takes precedence over `since_seq`.",
       "title": "Last-Event-Id"
      },
      "description": "Standard SSE auto-reconnect cursor (integer `seq`). Takes precedence over `since_seq`."
     }
    ],
    "responses": {
     "200": {
      "description": "A `text/event-stream` response. Each frame carries one event envelope as JSON in `data:` with its `seq` in `id:`. The connection stays open; reconnect with the last `seq` to resume without loss or duplicates.",
      "content": {
       "application/json": {
        "schema": {}
       },
       "text/event-stream": {
        "schema": {
         "type": "string"
        },
        "example": "id: 42\ndata: {\"event_id\": \"...\", \"event_type\": \"task.updated\", ...}\n\n"
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/transfers": {
   "get": {
    "tags": [
     "transfers"
    ],
    "summary": "List Transfers",
    "description": "List transfer metadata visible to the caller (sender, recipient, broadcast, or privileged role). The list silently omits inaccessible transfers instead of erroring.",
    "operationId": "list_transfers_api_v1_transfers_get",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "project_id",
      "in": "query",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string",
         "format": "uuid"
        },
        {
         "type": "null"
        }
       ],
       "title": "Project Id"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "type": "array",
         "items": {
          "$ref": "#/components/schemas/Transfer"
         },
         "title": "Response List Transfers Api V1 Transfers Get"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   },
   "post": {
    "tags": [
     "transfers"
    ],
    "summary": "Create Transfer",
    "description": "Start a transfer: declares filename, size and participants, and reserves quota. Requires a writer role. Accepts `Idempotency-Key` for safe retries. File bytes never pass through this API: the next steps hand back short-lived signed URLs, and the client uploads directly to object storage. Oversize files fail with `413 transfer_too_large`; an exhausted quota fails with `507 quota_exceeded` (check `GET /transfers/consumption` first for large uploads).",
    "operationId": "create_transfer_api_v1_transfers_post",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "Idempotency-Key",
      "in": "header",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string"
        },
        {
         "type": "null"
        }
       ],
       "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`.",
       "title": "Idempotency-Key"
      },
      "description": "Optional replay key for safe retries (timeouts, reconnects, offline queue replay). Send a caller-generated unique value per intended resource: replaying the same key with the identical body returns the original response instead of creating a duplicate, even under concurrent retries. Replaying the same key with a different body is a client error (`409 idempotency_key_payload_mismatch`) \u2014 always resend the exact same body when retrying. A key whose creation never completed may briefly answer `409 idempotency_key_in_progress`; retry identically. `POST /events` does not use this header (the client-generated `event_id` plays that role instead), and neither do `POST /machines` and `POST /users`."
     }
    ],
    "requestBody": {
     "required": true,
     "content": {
      "application/json": {
       "schema": {
        "$ref": "#/components/schemas/TransferCreate"
       }
      }
     }
    },
    "responses": {
     "201": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/Transfer"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "409": {
      "description": "Replay key problem, no duplicate was created: either the same `Idempotency-Key` was reused with a different body (`idempotency_key_payload_mismatch` \u2014 resend the exact original body) or a previous creation with this key is still completing (`idempotency_key_in_progress` \u2014 retry identically after a short delay).",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "idempotency_key_payload_mismatch"
         }
        }
       }
      }
     },
     "413": {
      "description": "Transfer size exceeds the per-transfer maximum. Split the file or use the multipart upload path; `max_size_bytes` is the limit that applied.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "transfer_too_large",
          "size_bytes": 1073741824,
          "max_size_bytes": 536870912
         }
        }
       }
      }
     },
     "507": {
      "description": "Project (or unscoped) storage quota exceeded. Check `GET /transfers/consumption` for `remaining_bytes` before a large upload; between the check and the creation another upload may still win the race.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "quota_exceeded",
          "project_id": "00000000-0000-0000-0000-000000000000",
          "consumed_bytes": 900,
          "requested_bytes": 200,
          "quota_bytes": 1000
         }
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/transfers/consumption": {
   "get": {
    "tags": [
     "transfers"
    ],
    "summary": "Get Consumption",
    "description": "Read quota consumption (`consumed_bytes`, `quota_bytes`, `remaining_bytes`) for a project, or for the unscoped bucket when no project is given. Any authenticated machine may read. Call before a large upload to avoid a rejected creation \u2014 the reading is advisory, another upload may still win the race.",
    "operationId": "get_consumption_api_v1_transfers_consumption_get",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "project_id",
      "in": "query",
      "required": false,
      "schema": {
       "anyOf": [
        {
         "type": "string",
         "format": "uuid"
        },
        {
         "type": "null"
        }
       ],
       "title": "Project Id"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/TransferConsumption"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/transfers/{transfer_id}": {
   "get": {
    "tags": [
     "transfers"
    ],
    "summary": "Get Transfer",
    "description": "Get one transfer's metadata. Only the sender, the recipient, broadcast recipients, or a privileged role may read it \u2014 anyone else receives `403 forbidden`, never 404.",
    "operationId": "get_transfer_api_v1_transfers__transfer_id__get",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "transfer_id",
      "in": "path",
      "required": true,
      "schema": {
       "type": "string",
       "format": "uuid",
       "title": "Transfer Id"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/Transfer"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "404": {
      "description": "No such resource. Unknown ids return 404; access to an existing but unauthorized transfer returns 403 instead, never 404.",
      "content": {
       "application/json": {
        "example": {
         "detail": "task not found"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   },
   "delete": {
    "tags": [
     "transfers"
    ],
    "summary": "Delete Transfer",
    "description": "Delete a transfer and its stored bytes. Only the sender (or a privileged role) may delete. Non-expired transfers are never removed automatically.",
    "operationId": "delete_transfer_api_v1_transfers__transfer_id__delete",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "transfer_id",
      "in": "path",
      "required": true,
      "schema": {
       "type": "string",
       "format": "uuid",
       "title": "Transfer Id"
      }
     }
    ],
    "responses": {
     "204": {
      "description": "Successful Response"
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "404": {
      "description": "No such resource. Unknown ids return 404; access to an existing but unauthorized transfer returns 403 instead, never 404.",
      "content": {
       "application/json": {
        "example": {
         "detail": "task not found"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/transfers/{transfer_id}/upload/initiate": {
   "post": {
    "tags": [
     "transfers"
    ],
    "summary": "Initiate Upload",
    "description": "Begin the byte upload and receive signed URL(s) valid for minutes. Two paths: small files use a single direct upload \u2014 send the file's MD5 (base64) as `content_md5`, it is embedded in the signed URL and storage itself rejects mismatched bytes (`422 missing_content_md5` when omitted); large files use multipart upload in 64-128 MiB chunks, each part going straight to storage. Only the sender (or a privileged role) may upload. Keep completed part numbers and their ETags locally so an interrupted multipart upload resumes without re-sending finished parts. Re-initiating an already `ready` transfer fails \u2014 completed uploads are never replaceable.",
    "operationId": "initiate_upload_api_v1_transfers__transfer_id__upload_initiate_post",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "transfer_id",
      "in": "path",
      "required": true,
      "schema": {
       "type": "string",
       "format": "uuid",
       "title": "Transfer Id"
      }
     }
    ],
    "requestBody": {
     "content": {
      "application/json": {
       "schema": {
        "anyOf": [
         {
          "$ref": "#/components/schemas/UploadInitiateRequest"
         },
         {
          "type": "null"
         }
        ],
        "title": "Body"
       }
      }
     }
    },
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/UploadInitiateResponse"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "404": {
      "description": "No such resource. Unknown ids return 404; access to an existing but unauthorized transfer returns 403 instead, never 404.",
      "content": {
       "application/json": {
        "example": {
         "detail": "task not found"
        }
       }
      }
     },
     "409": {
      "description": "Upload state conflict: the transfer is already `ready` (`transfer_already_ready`, uploads are never replaceable), the multipart `upload_id` is unknown for this transfer (`unknown_upload_id` \u2014 discard local upload state and start over with `upload/initiate`), or the resumed part size differs from the server constant (`part_size_mismatch`).",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "transfer_already_ready"
         }
        }
       }
      }
     },
     "422": {
      "description": "Upload integrity or shape rejected, nothing marked ready: small uploads require `content_md5` at initiate (`missing_content_md5`); the completed size must equal the size declared at creation (`size_mismatch` / `object_not_found`); on the single-upload path the stored bytes are re-verified against the presigned checksum (`content_md5_mismatch`). Part numbers outside the multipart bounds fail as `invalid_part_number`.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "missing_content_md5"
         }
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/transfers/{transfer_id}/upload/refresh-parts": {
   "post": {
    "tags": [
     "transfers"
    ],
    "summary": "Refresh Upload Parts",
    "description": "Resume helper for an interrupted multipart upload: reports which parts storage already holds (server truth) and re-signs URLs for the missing parts only. Adopt the returned part list as the new local state. An unknown `upload_id` means the server-side upload is gone \u2014 discard local state and restart from `upload/initiate`.",
    "operationId": "refresh_upload_parts_api_v1_transfers__transfer_id__upload_refresh_parts_post",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "transfer_id",
      "in": "path",
      "required": true,
      "schema": {
       "type": "string",
       "format": "uuid",
       "title": "Transfer Id"
      }
     }
    ],
    "requestBody": {
     "required": true,
     "content": {
      "application/json": {
       "schema": {
        "$ref": "#/components/schemas/UploadPartsRefreshRequest"
       }
      }
     }
    },
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/UploadPartsRefreshResponse"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "404": {
      "description": "No such resource. Unknown ids return 404; access to an existing but unauthorized transfer returns 403 instead, never 404.",
      "content": {
       "application/json": {
        "example": {
         "detail": "task not found"
        }
       }
      }
     },
     "409": {
      "description": "Upload state conflict: the transfer is already `ready` (`transfer_already_ready`, uploads are never replaceable), the multipart `upload_id` is unknown for this transfer (`unknown_upload_id` \u2014 discard local upload state and start over with `upload/initiate`), or the resumed part size differs from the server constant (`part_size_mismatch`).",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "transfer_already_ready"
         }
        }
       }
      }
     },
     "422": {
      "description": "Upload integrity or shape rejected, nothing marked ready: small uploads require `content_md5` at initiate (`missing_content_md5`); the completed size must equal the size declared at creation (`size_mismatch` / `object_not_found`); on the single-upload path the stored bytes are re-verified against the presigned checksum (`content_md5_mismatch`). Part numbers outside the multipart bounds fail as `invalid_part_number`.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "missing_content_md5"
         }
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/transfers/{transfer_id}/upload/complete": {
   "post": {
    "tags": [
     "transfers"
    ],
    "summary": "Complete Upload",
    "description": "Finish the upload and mark the transfer `ready`. The declared size must equal the size given at creation (the value quota was checked against), and on the single-upload path the stored bytes are re-verified against the presigned checksum \u2014 any divergence fails instead of silently marking ready. Only the sender (or a privileged role) may complete.",
    "operationId": "complete_upload_api_v1_transfers__transfer_id__upload_complete_post",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "transfer_id",
      "in": "path",
      "required": true,
      "schema": {
       "type": "string",
       "format": "uuid",
       "title": "Transfer Id"
      }
     }
    ],
    "requestBody": {
     "required": true,
     "content": {
      "application/json": {
       "schema": {
        "$ref": "#/components/schemas/UploadCompleteRequest"
       }
      }
     }
    },
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/Transfer"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "404": {
      "description": "No such resource. Unknown ids return 404; access to an existing but unauthorized transfer returns 403 instead, never 404.",
      "content": {
       "application/json": {
        "example": {
         "detail": "task not found"
        }
       }
      }
     },
     "422": {
      "description": "Upload integrity or shape rejected, nothing marked ready: small uploads require `content_md5` at initiate (`missing_content_md5`); the completed size must equal the size declared at creation (`size_mismatch` / `object_not_found`); on the single-upload path the stored bytes are re-verified against the presigned checksum (`content_md5_mismatch`). Part numbers outside the multipart bounds fail as `invalid_part_number`.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "missing_content_md5"
         }
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/transfers/{transfer_id}/download-url": {
   "post": {
    "tags": [
     "transfers"
    ],
    "summary": "Get Download Url",
    "description": "Get a short-lived presigned download URL (minutes, never the file bytes). Only the sender, the recipient, broadcast recipients, or a privileged role may obtain it. Downloads support HTTP `Range`, so a partial download resumes where it stopped.",
    "operationId": "get_download_url_api_v1_transfers__transfer_id__download_url_post",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "transfer_id",
      "in": "path",
      "required": true,
      "schema": {
       "type": "string",
       "format": "uuid",
       "title": "Transfer Id"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/DownloadUrlResponse"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "404": {
      "description": "No such resource. Unknown ids return 404; access to an existing but unauthorized transfer returns 403 instead, never 404.",
      "content": {
       "application/json": {
        "example": {
         "detail": "task not found"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/machines": {
   "post": {
    "tags": [
     "machines"
    ],
    "summary": "Create Machine",
    "description": "Provision a new machine credential. Requires the admin role. The credential is returned in clear text exactly once \u2014 store it immediately, it is never readable again. Deliberately not replayable: no `Idempotency-Key` (a replayable credential creation would persist the secret). The very first machine is created out of band, never through this endpoint.",
    "operationId": "create_machine_api_v1_machines_post",
    "requestBody": {
     "content": {
      "application/json": {
       "schema": {
        "$ref": "#/components/schemas/MachineCreate"
       }
      }
     },
     "required": true
    },
    "responses": {
     "201": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/MachineCreated"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    },
    "security": [
     {
      "MachineBearer": []
     }
    ]
   }
  },
  "/api/v1/machines/{machine_id}/revoke": {
   "post": {
    "tags": [
     "machines"
    ],
    "summary": "Revoke Machine",
    "description": "Revoke a machine credential immediately. Requires the admin role. Revocation takes effect on the next request \u2014 there is no grace period and no rotation to manage.",
    "operationId": "revoke_machine_api_v1_machines__machine_id__revoke_post",
    "security": [
     {
      "MachineBearer": []
     }
    ],
    "parameters": [
     {
      "name": "machine_id",
      "in": "path",
      "required": true,
      "schema": {
       "type": "string",
       "format": "uuid",
       "title": "Machine Id"
      }
     }
    ],
    "responses": {
     "200": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/Machine"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "404": {
      "description": "No such resource. Unknown ids return 404; access to an existing but unauthorized transfer returns 403 instead, never 404.",
      "content": {
       "application/json": {
        "example": {
         "detail": "task not found"
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    }
   }
  },
  "/api/v1/users": {
   "post": {
    "tags": [
     "users"
    ],
    "summary": "Create User",
    "description": "Provision a new user with a role (`admin`, `developer`, `agent`, or `readonly` \u2014 the role alone decides what the user's machines may do). Requires the admin role. Emails are unique: reusing one fails with 409. Deliberately not replayable: no `Idempotency-Key`. The very first (admin) user is created out of band, never through this endpoint.",
    "operationId": "create_user_api_v1_users_post",
    "requestBody": {
     "content": {
      "application/json": {
       "schema": {
        "$ref": "#/components/schemas/UserCreate"
       }
      }
     },
     "required": true
    },
    "responses": {
     "201": {
      "description": "Successful Response",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/User"
        }
       }
      }
     },
     "401": {
      "description": "Missing, invalid or revoked credential. Send `Authorization: Bearer <machine-token>` for a machine, or `Authorization: Bearer <jwt>` obtained from `POST /auth/token` for a human dashboard user; provision the machine token out of band before calling.",
      "content": {
       "application/json": {
        "example": {
         "detail": "missing bearer token"
        }
       }
      }
     },
     "403": {
      "description": "Authenticated but not allowed. The caller's role or resource ownership does not permit this action (`resource` names the object kind, `action` the attempted operation). A 403 is final: retrying the same call changes nothing, and a queued offline operation that replays into a 403 is dead-lettered, never retried. Reads stay fully available; only the listed write operations can return this.",
      "content": {
       "application/json": {
        "example": {
         "detail": {
          "error_code": "forbidden",
          "resource": "task",
          "action": "write"
         }
        }
       }
      }
     },
     "422": {
      "description": "Validation Error",
      "content": {
       "application/json": {
        "schema": {
         "$ref": "#/components/schemas/HTTPValidationError"
        }
       }
      }
     }
    },
    "security": [
     {
      "MachineBearer": []
     }
    ]
   }
  }
 },
 "components": {
  "schemas": {
   "AIWorkLog": {
    "properties": {
     "id": {
      "type": "string",
      "format": "uuid",
      "title": "Id"
     },
     "task_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Task Id"
     },
     "project_id": {
      "type": "string",
      "format": "uuid",
      "title": "Project Id"
     },
     "agent_id": {
      "type": "string",
      "format": "uuid",
      "title": "Agent Id"
     },
     "machine_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Machine Id"
     },
     "summary": {
      "type": "string",
      "title": "Summary"
     },
     "status": {
      "$ref": "#/components/schemas/AIWorkStatus",
      "default": "started"
     },
     "changed_files": {
      "items": {
       "type": "string"
      },
      "type": "array",
      "title": "Changed Files",
      "default": []
     },
     "tests_run": {
      "items": {
       "type": "string"
      },
      "type": "array",
      "title": "Tests Run",
      "default": []
     },
     "started_at": {
      "type": "string",
      "format": "date-time",
      "title": "Started At"
     },
     "ended_at": {
      "anyOf": [
       {
        "type": "string",
        "format": "date-time"
       },
       {
        "type": "null"
       }
      ],
      "title": "Ended At"
     },
     "agent_profile": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Agent Profile"
     },
     "harness": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Harness"
     },
     "provider": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Provider"
     },
     "model": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Model"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "id",
     "project_id",
     "agent_id",
     "summary",
     "started_at"
    ],
    "title": "AIWorkLog",
    "description": "A work entry. `agent_profile`, `harness`, `provider` and `model` are\noptional additive observability metadata \u2014 open strings snapshotting the\nruntime that produced the work, never whitelisted, never an\nauthorization or capability input."
   },
   "AIWorkLogCreate": {
    "properties": {
     "task_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Task Id"
     },
     "project_id": {
      "type": "string",
      "format": "uuid",
      "title": "Project Id"
     },
     "agent_id": {
      "type": "string",
      "format": "uuid",
      "title": "Agent Id"
     },
     "machine_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Machine Id"
     },
     "summary": {
      "type": "string",
      "title": "Summary"
     },
     "agent_profile": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Agent Profile"
     },
     "harness": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Harness"
     },
     "provider": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Provider"
     },
     "model": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Model"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "project_id",
     "agent_id",
     "summary"
    ],
    "title": "AIWorkLogCreate"
   },
   "AIWorkLogUpdate": {
    "properties": {
     "summary": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Summary"
     },
     "status": {
      "anyOf": [
       {
        "$ref": "#/components/schemas/AIWorkStatus"
       },
       {
        "type": "null"
       }
      ]
     },
     "changed_files": {
      "anyOf": [
       {
        "items": {
         "type": "string"
        },
        "type": "array"
       },
       {
        "type": "null"
       }
      ],
      "title": "Changed Files"
     },
     "tests_run": {
      "anyOf": [
       {
        "items": {
         "type": "string"
        },
        "type": "array"
       },
       {
        "type": "null"
       }
      ],
      "title": "Tests Run"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "title": "AIWorkLogUpdate"
   },
   "AIWorkStatus": {
    "type": "string",
    "enum": [
     "started",
     "completed",
     "failed",
     "review_requested",
     "approved",
     "changes_requested"
    ],
    "title": "AIWorkStatus",
    "description": "Work entry lifecycle. `approved` and `changes_requested` are the only\nvalid exits from `review_requested`, and only a privileged role may set\nthem \u2014 never the owning agent or machine (nobody approves their own\nwork)."
   },
   "Agent": {
    "properties": {
     "created_at": {
      "type": "string",
      "format": "date-time",
      "title": "Created At"
     },
     "updated_at": {
      "type": "string",
      "format": "date-time",
      "title": "Updated At"
     },
     "version": {
      "type": "integer",
      "title": "Version"
     },
     "id": {
      "type": "string",
      "format": "uuid",
      "title": "Id"
     },
     "machine_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Machine Id"
     },
     "display_name": {
      "type": "string",
      "title": "Display Name"
     },
     "agent_kind": {
      "type": "string",
      "title": "Agent Kind"
     },
     "agent_profile": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Agent Profile"
     },
     "harness": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Harness"
     },
     "provider": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Provider"
     },
     "model": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Model"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "created_at",
     "updated_at",
     "version",
     "id",
     "display_name",
     "agent_kind"
    ],
    "title": "Agent",
    "description": "Provenance identity attached to one machine: who did the work, for\naudit and attribution. Never an authorization input \u2014 permissions come\nfrom the machine owner's role alone. `agent_profile`, `harness`,\n`provider` and `model` are optional additive observability metadata:\nopen strings, never whitelisted, never a capability or compatibility\ncondition, never read to make a decision."
   },
   "AgentCreate": {
    "properties": {
     "display_name": {
      "type": "string",
      "title": "Display Name"
     },
     "agent_kind": {
      "type": "string",
      "title": "Agent Kind",
      "default": ""
     },
     "agent_profile": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Agent Profile"
     },
     "harness": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Harness"
     },
     "provider": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Provider"
     },
     "model": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Model"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "display_name"
    ],
    "title": "AgentCreate",
    "description": "Public registration of a provenance identity: `machine_id` is always\nderived from the authenticated machine, never client-supplied.\n`display_name` and `agent_kind` are free-form metadata \u2014 never\nauthorization inputs, never the canonical identity (the\nserver-generated `Agent.id` is). `agent_profile`, `harness`, `provider`\nand `model` are optional open-string observability metadata: any value\nis accepted, unknown values are never rejected, and none of them is ever\nrequired."
   },
   "AgentResolutionRequest": {
    "properties": {
     "stable_key": {
      "type": "string",
      "maxLength": 200,
      "minLength": 1,
      "title": "Stable Key"
     },
     "project_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Project Id"
     },
     "session_overrides": {
      "items": {
       "$ref": "#/components/schemas/SessionRuntimeOverride"
      },
      "type": "array",
      "title": "Session Overrides",
      "default": []
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "stable_key"
    ],
    "title": "AgentResolutionRequest",
    "description": "Canonical HTTP (and future MCP) input for full agent resolution\n(P7): which `AgentDefinition` (`stable_key`, kind fixed \u2014\nthe engine only resolves agent definitions), in which project\ncontext, with which ephemeral session overrides. Identity always\ncomes from auth, never from this body. The response is the complete\n`ResolvedAgentDefinition`, never a simplified shape."
   },
   "BindingRelation": {
    "type": "string",
    "enum": [
     "requires_model_profile",
     "uses_skill",
     "applies_rule",
     "composes_agent",
     "references_workflow",
     "refines_skill_rule"
    ],
    "title": "BindingRelation",
    "description": "Closed vocabulary of Library Binding types.\n\nEvery allowed `(source kind, target kind)` couple maps to exactly one\nrelation (see `_BINDING_MATRIX`) \u2014 the relation is a label on that\ncouple, never a free-form string. Unknown values are rejected by\nPydantic before any existence check runs."
   },
   "Build": {
    "properties": {
     "created_at": {
      "type": "string",
      "format": "date-time",
      "title": "Created At"
     },
     "updated_at": {
      "type": "string",
      "format": "date-time",
      "title": "Updated At"
     },
     "id": {
      "type": "string",
      "format": "uuid",
      "title": "Id"
     },
     "project_id": {
      "type": "string",
      "format": "uuid",
      "title": "Project Id"
     },
     "task_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Task Id"
     },
     "github_integration_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Github Integration Id"
     },
     "workflow_run_id": {
      "type": "integer",
      "title": "Workflow Run Id"
     },
     "workflow_name": {
      "type": "string",
      "title": "Workflow Name"
     },
     "run_number": {
      "type": "integer",
      "title": "Run Number"
     },
     "branch": {
      "type": "string",
      "title": "Branch"
     },
     "commit_sha": {
      "type": "string",
      "title": "Commit Sha"
     },
     "pr_number": {
      "anyOf": [
       {
        "type": "integer"
       },
       {
        "type": "null"
       }
      ],
      "title": "Pr Number"
     },
     "status": {
      "$ref": "#/components/schemas/BuildStatus",
      "default": "queued"
     },
     "conclusion": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Conclusion"
     },
     "html_url": {
      "type": "string",
      "title": "Html Url"
     },
     "actor_login": {
      "type": "string",
      "title": "Actor Login"
     },
     "started_at": {
      "anyOf": [
       {
        "type": "string",
        "format": "date-time"
       },
       {
        "type": "null"
       }
      ],
      "title": "Started At"
     },
     "completed_at": {
      "anyOf": [
       {
        "type": "string",
        "format": "date-time"
       },
       {
        "type": "null"
       }
      ],
      "title": "Completed At"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "created_at",
     "updated_at",
     "id",
     "project_id",
     "workflow_run_id",
     "workflow_name",
     "run_number",
     "branch",
     "commit_sha",
     "html_url",
     "actor_login"
    ],
    "title": "Build",
    "description": "A CI build observed on a project's GitHub repository.\nAdditive-only: new optional fields may appear, existing ones are never\nrenamed or removed."
   },
   "BuildStatus": {
    "type": "string",
    "enum": [
     "queued",
     "in_progress",
     "succeeded",
     "failed"
    ],
    "title": "BuildStatus",
    "description": "Build lifecycle, mirrored from the `build.*` event types. Transitions\nonly ever move forward (`queued` -> `in_progress` -> `succeeded`/`failed`)\nand are written by the server (GitHub webhook, reconcile worker) \u2014 no\nclient PATCH endpoint exists."
   },
   "CapabilityRequirement": {
    "properties": {
     "reasoning": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Reasoning"
     },
     "coding": {
      "type": "boolean",
      "title": "Coding",
      "default": false
     },
     "context_window_min": {
      "anyOf": [
       {
        "type": "integer"
       },
       {
        "type": "null"
       }
      ],
      "title": "Context Window Min"
     },
     "tools_required": {
      "items": {
       "type": "string"
      },
      "type": "array",
      "title": "Tools Required",
      "default": []
     },
     "multimodal": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Multimodal"
     },
     "local_compatible": {
      "type": "boolean",
      "title": "Local Compatible",
      "default": false
     },
     "cost": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Cost"
     },
     "latency": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Latency"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "title": "CapabilityRequirement",
    "description": "Vendor-neutral model requirements (gate P0 deliverable, enforced from\nP3). All dimensions are open strings/numbers, never vendor enums,\nwhitelists or commercial rankings. A `ModelProfile` resource carries one\nof these in its version `content` (see `ModelProfileContent`)."
   },
   "CapabilitySource": {
    "type": "string",
    "enum": [
     "declared"
    ],
    "title": "CapabilitySource",
    "description": "Where registered capabilities come from (P6 registry).\n\nP6 MVP only ever stores `declared` (explicitly registered, no discovery,\nno detection, no adapter report). The enum \u2014 not a closed vendor list \u2014\nis the extension point for future `detected` / `adapter-reported`\nsources; any other value is fail-closed until its semantics are defined."
   },
   "ClaimStatus": {
    "type": "string",
    "enum": [
     "active",
     "released",
     "expired"
    ],
    "title": "ClaimStatus",
    "description": "A claim past `expires_at` is not active regardless of stored status.\nA claim is a soft lock: it warns, it never blocks a Git operation or a\nfile write."
   },
   "Decision": {
    "properties": {
     "id": {
      "type": "string",
      "format": "uuid",
      "title": "Id"
     },
     "readable_id": {
      "type": "string",
      "title": "Readable Id"
     },
     "project_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Project Id"
     },
     "task_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Task Id"
     },
     "title": {
      "type": "string",
      "title": "Title"
     },
     "body": {
      "type": "string",
      "title": "Body"
     },
     "status": {
      "$ref": "#/components/schemas/DecisionStatus",
      "default": "proposed"
     },
     "proposed_by_type": {
      "type": "string",
      "title": "Proposed By Type"
     },
     "proposed_by_id": {
      "type": "string",
      "format": "uuid",
      "title": "Proposed By Id"
     },
     "created_at": {
      "type": "string",
      "format": "date-time",
      "title": "Created At"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "id",
     "readable_id",
     "title",
     "body",
     "proposed_by_type",
     "proposed_by_id",
     "created_at"
    ],
    "title": "Decision",
    "description": "A recorded project decision with a stable human-readable id\n(`readable_id`, e.g. DEC-XXXX) alongside its internal UUID."
   },
   "DecisionCreate": {
    "properties": {
     "project_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Project Id"
     },
     "task_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Task Id"
     },
     "title": {
      "type": "string",
      "title": "Title"
     },
     "body": {
      "type": "string",
      "title": "Body"
     },
     "proposed_by_type": {
      "type": "string",
      "title": "Proposed By Type"
     },
     "proposed_by_id": {
      "type": "string",
      "format": "uuid",
      "title": "Proposed By Id"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "title",
     "body",
     "proposed_by_type",
     "proposed_by_id"
    ],
    "title": "DecisionCreate"
   },
   "DecisionStatus": {
    "type": "string",
    "enum": [
     "proposed",
     "accepted",
     "superseded"
    ],
    "title": "DecisionStatus"
   },
   "DependencyPin": {
    "properties": {
     "kind": {
      "$ref": "#/components/schemas/LibraryKind"
     },
     "stable_key": {
      "type": "string",
      "title": "Stable Key"
     },
     "version": {
      "type": "integer",
      "title": "Version"
     },
     "relation": {
      "anyOf": [
       {
        "$ref": "#/components/schemas/BindingRelation"
       },
       {
        "type": "null"
       }
      ]
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "kind",
     "stable_key",
     "version"
    ],
    "title": "DependencyPin",
    "description": "A version-pinned reference to another library resource version.\n\n`relation` names the Library Binding type. When omitted it\nis inferred server-side from the unique matrix mapping for the\n`(source kind, target kind)` couple; an explicitly wrong relation for an\notherwise allowed couple is rejected with `422 invalid_binding`."
   },
   "DownloadUrlResponse": {
    "properties": {
     "transfer_id": {
      "type": "string",
      "format": "uuid",
      "title": "Transfer Id"
     },
     "download_url": {
      "type": "string",
      "title": "Download Url"
     },
     "expires_at": {
      "type": "string",
      "format": "date-time",
      "title": "Expires At"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "transfer_id",
     "download_url",
     "expires_at"
    ],
    "title": "DownloadUrlResponse"
   },
   "EventCreate": {
    "properties": {
     "event_id": {
      "type": "string",
      "format": "uuid",
      "title": "Event Id"
     },
     "event_type": {
      "$ref": "#/components/schemas/EventType"
     },
     "project_id": {
      "type": "string",
      "format": "uuid",
      "title": "Project Id"
     },
     "task_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Task Id"
     },
     "machine_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Machine Id"
     },
     "actor_type": {
      "type": "string",
      "enum": [
       "user",
       "agent",
       "system"
      ],
      "title": "Actor Type"
     },
     "actor_id": {
      "type": "string",
      "format": "uuid",
      "title": "Actor Id"
     },
     "client_timestamp": {
      "type": "string",
      "format": "date-time",
      "title": "Client Timestamp"
     },
     "payload": {
      "additionalProperties": true,
      "type": "object",
      "title": "Payload",
      "default": {}
     },
     "schema_version": {
      "type": "integer",
      "title": "Schema Version",
      "default": 1
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "event_id",
     "event_type",
     "project_id",
     "actor_type",
     "actor_id",
     "client_timestamp"
    ],
    "title": "EventCreate",
    "description": "POST /events request body. `event_id` is client-generated and stable\nacross retries \u2014 it IS the idempotency key for events: replaying the\nsame event_id returns the original stored event rather than creating a\nduplicate."
   },
   "EventEnvelope": {
    "properties": {
     "event_id": {
      "type": "string",
      "format": "uuid",
      "title": "Event Id"
     },
     "event_type": {
      "$ref": "#/components/schemas/EventType"
     },
     "project_id": {
      "type": "string",
      "format": "uuid",
      "title": "Project Id"
     },
     "task_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Task Id"
     },
     "machine_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Machine Id"
     },
     "actor_type": {
      "type": "string",
      "enum": [
       "user",
       "agent",
       "system"
      ],
      "title": "Actor Type"
     },
     "actor_id": {
      "type": "string",
      "format": "uuid",
      "title": "Actor Id"
     },
     "client_timestamp": {
      "type": "string",
      "format": "date-time",
      "title": "Client Timestamp"
     },
     "server_timestamp": {
      "type": "string",
      "format": "date-time",
      "title": "Server Timestamp"
     },
     "payload": {
      "additionalProperties": true,
      "type": "object",
      "title": "Payload",
      "default": {}
     },
     "schema_version": {
      "type": "integer",
      "title": "Schema Version",
      "default": 1
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "event_id",
     "event_type",
     "project_id",
     "actor_type",
     "actor_id",
     "client_timestamp",
     "server_timestamp"
    ],
    "title": "EventEnvelope",
    "description": "Fixed envelope \u2014 only `payload` grows across `schema_version` bumps;\nevery other field keeps its shape. Unknown event types or extra payload\nfields must be tolerated, never rejected."
   },
   "EventType": {
    "type": "string",
    "enum": [
     "project.created",
     "task.created",
     "task.started",
     "task.updated",
     "task.blocked",
     "task.completed",
     "session.started",
     "session.ended",
     "resource.claimed",
     "resource.renewed",
     "resource.released",
     "resource.conflict",
     "decision.proposed",
     "decision.created",
     "library.version.created",
     "library.version.activated",
     "library.resource.deprecated",
     "library.lock.set",
     "library.lock.released",
     "agent.started",
     "agent.stopped",
     "ai_work.started",
     "ai_work.completed",
     "ai_work.failed",
     "ai_work.review_requested",
     "ai_work.approved",
     "ai_work.changes_requested",
     "git.commit",
     "git.branch.changed",
     "git.pr.opened",
     "git.pr.merged",
     "graph.updated",
     "memory.proposed",
     "memory.updated",
     "godot.started",
     "godot.stopped",
     "recording.started",
     "recording.finished",
     "recording.marker.created",
     "build.started",
     "build.succeeded",
     "build.failed",
     "producer.job.requested",
     "producer.job.completed",
     "producer.job.failed",
     "transfer.created",
     "transfer.uploading",
     "transfer.ready",
     "transfer.downloaded",
     "transfer.expired",
     "transfer.deleted",
     "marketing.candidate.created",
     "marketing.post.published"
    ],
    "title": "EventType",
    "description": "Fixed dotted names (`domain.verb`: task.created, session.ended, ...).\nAdditive-only: new members may appear, existing ones are never renamed\nor removed \u2014 readers should tolerate unknown types."
   },
   "GitHubIntegration": {
    "properties": {
     "created_at": {
      "type": "string",
      "format": "date-time",
      "title": "Created At"
     },
     "updated_at": {
      "type": "string",
      "format": "date-time",
      "title": "Updated At"
     },
     "id": {
      "type": "string",
      "format": "uuid",
      "title": "Id"
     },
     "project_id": {
      "type": "string",
      "format": "uuid",
      "title": "Project Id"
     },
     "repo_full_name": {
      "type": "string",
      "title": "Repo Full Name"
     },
     "default_branch": {
      "type": "string",
      "title": "Default Branch",
      "default": "main"
     },
     "enabled": {
      "type": "boolean",
      "title": "Enabled",
      "default": true
     },
     "created_by_user_id": {
      "type": "string",
      "format": "uuid",
      "title": "Created By User Id"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "created_at",
     "updated_at",
     "id",
     "project_id",
     "repo_full_name",
     "created_by_user_id"
    ],
    "title": "GitHubIntegration",
    "description": "Per-project GitHub wiring. At most one row per project in\nv1 (`project_id` unique) \u2014 the webhook secret itself is never stored\nhere: it is a single process-wide env var (`STUDIO_GITHUB_WEBHOOK_SECRET`),\nnever logged, never returned by the API."
   },
   "GitHubIntegrationCreate": {
    "properties": {
     "project_id": {
      "type": "string",
      "format": "uuid",
      "title": "Project Id"
     },
     "repo_full_name": {
      "type": "string",
      "title": "Repo Full Name"
     },
     "default_branch": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Default Branch"
     },
     "enabled": {
      "anyOf": [
       {
        "type": "boolean"
       },
       {
        "type": "null"
       }
      ],
      "title": "Enabled"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "project_id",
     "repo_full_name"
    ],
    "title": "GitHubIntegrationCreate"
   },
   "GitHubIntegrationUpdate": {
    "properties": {
     "repo_full_name": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Repo Full Name"
     },
     "default_branch": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Default Branch"
     },
     "enabled": {
      "anyOf": [
       {
        "type": "boolean"
       },
       {
        "type": "null"
       }
      ],
      "title": "Enabled"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "title": "GitHubIntegrationUpdate"
   },
   "GitHubWebhookResult": {
    "properties": {
     "status": {
      "type": "string",
      "title": "Status"
     },
     "github_event": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Github Event"
     },
     "build_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Build Id"
     },
     "pr_number": {
      "anyOf": [
       {
        "type": "integer"
       },
       {
        "type": "null"
       }
      ],
      "title": "Pr Number"
     },
     "detail": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Detail"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "status"
    ],
    "title": "GitHubWebhookResult",
    "description": "Outcome of one verified GitHub delivery. `status` is `accepted`\n(events/builds written or converged) or `ignored` (unknown event,\nunconfigured repository, disabled integration) \u2014 both answer `200`/`202`\nat HTTP level, never `500`."
   },
   "HTTPValidationError": {
    "properties": {
     "detail": {
      "items": {
       "$ref": "#/components/schemas/ValidationError"
      },
      "type": "array",
      "title": "Detail"
     }
    },
    "type": "object",
    "title": "HTTPValidationError"
   },
   "HeartbeatRequest": {
    "properties": {
     "machine_id": {
      "type": "string",
      "format": "uuid",
      "title": "Machine Id"
     },
     "agent_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Agent Id"
     },
     "client_timestamp": {
      "type": "string",
      "format": "date-time",
      "title": "Client Timestamp"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "machine_id",
     "client_timestamp"
    ],
    "title": "HeartbeatRequest"
   },
   "HeartbeatResponse": {
    "properties": {
     "machine_id": {
      "type": "string",
      "format": "uuid",
      "title": "Machine Id"
     },
     "status": {
      "$ref": "#/components/schemas/MachineStatus"
     },
     "last_seen_at": {
      "type": "string",
      "format": "date-time",
      "title": "Last Seen At"
     },
     "server_timestamp": {
      "type": "string",
      "format": "date-time",
      "title": "Server Timestamp"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "machine_id",
     "status",
     "last_seen_at",
     "server_timestamp"
    ],
    "title": "HeartbeatResponse"
   },
   "LibraryActivate": {
    "properties": {
     "version": {
      "type": "integer",
      "minimum": 1.0,
      "title": "Version"
     },
     "expected_resource_version": {
      "type": "integer",
      "title": "Expected Resource Version"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "version",
     "expected_resource_version"
    ],
    "title": "LibraryActivate",
    "description": "Explicit activation: moves `active_version`\nonly. `expected_resource_version` is the optimistic-concurrency guard\n(409 `version_conflict` on stale)."
   },
   "LibraryDeprecate": {
    "properties": {
     "expected_resource_version": {
      "type": "integer",
      "title": "Expected Resource Version"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "expected_resource_version"
    ],
    "title": "LibraryDeprecate",
    "description": "Deprecation replaces deletion: flips `status`, keeps\nhistory and `active_version`. Same optimistic-concurrency guard."
   },
   "LibraryKind": {
    "type": "string",
    "enum": [
     "rule",
     "skill",
     "agent_definition",
     "model_profile",
     "workflow"
    ],
    "title": "LibraryKind",
    "description": "Closed studio taxonomy (our own domain, not a vendor catalog):\nwhat kind of reusable definition a library resource is."
   },
   "LibraryLockCreate": {
    "properties": {
     "project_id": {
      "type": "string",
      "format": "uuid",
      "title": "Project Id"
     },
     "resource_id": {
      "type": "string",
      "format": "uuid",
      "title": "Resource Id"
     },
     "locked_version": {
      "type": "integer",
      "title": "Locked Version"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "project_id",
     "resource_id",
     "locked_version"
    ],
    "title": "LibraryLockCreate"
   },
   "LibraryProjectLock": {
    "properties": {
     "id": {
      "type": "string",
      "format": "uuid",
      "title": "Id"
     },
     "project_id": {
      "type": "string",
      "format": "uuid",
      "title": "Project Id"
     },
     "resource_id": {
      "type": "string",
      "format": "uuid",
      "title": "Resource Id"
     },
     "locked_version": {
      "type": "integer",
      "title": "Locked Version"
     },
     "created_by_user_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Created By User Id"
     },
     "created_at": {
      "type": "string",
      "format": "date-time",
      "title": "Created At"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "id",
     "project_id",
     "resource_id",
     "locked_version",
     "created_at"
    ],
    "title": "LibraryProjectLock",
    "description": "A project pins `(resource_id, locked_version)`: the lock key is the\ncanonical resource UUID, never `stable_key` alone."
   },
   "LibraryResource": {
    "properties": {
     "created_at": {
      "type": "string",
      "format": "date-time",
      "title": "Created At"
     },
     "updated_at": {
      "type": "string",
      "format": "date-time",
      "title": "Updated At"
     },
     "version": {
      "type": "integer",
      "title": "Version"
     },
     "id": {
      "type": "string",
      "format": "uuid",
      "title": "Id"
     },
     "kind": {
      "$ref": "#/components/schemas/LibraryKind"
     },
     "stable_key": {
      "type": "string",
      "title": "Stable Key"
     },
     "scope": {
      "$ref": "#/components/schemas/LibraryScope"
     },
     "status": {
      "$ref": "#/components/schemas/LibraryStatus",
      "default": "draft"
     },
     "active_version": {
      "type": "integer",
      "title": "Active Version",
      "default": 0
     },
     "owner_user_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Owner User Id"
     },
     "project_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Project Id"
     },
     "created_by_user_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Created By User Id"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "created_at",
     "updated_at",
     "version",
     "id",
     "kind",
     "stable_key",
     "scope"
    ],
    "title": "LibraryResource",
    "description": "The active pointer of one reusable definition.\n`active_version == 0` means no version has been activated yet.\nNever confused with `Agent` provenance identity."
   },
   "LibraryResourceCreate": {
    "properties": {
     "kind": {
      "$ref": "#/components/schemas/LibraryKind"
     },
     "stable_key": {
      "type": "string",
      "maxLength": 200,
      "minLength": 1,
      "title": "Stable Key"
     },
     "scope": {
      "$ref": "#/components/schemas/LibraryScope"
     },
     "project_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Project Id"
     },
     "title": {
      "type": "string",
      "minLength": 1,
      "title": "Title"
     },
     "description": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Description"
     },
     "content": {
      "additionalProperties": true,
      "type": "object",
      "title": "Content",
      "default": {}
     },
     "dependencies": {
      "items": {
       "$ref": "#/components/schemas/DependencyPin"
      },
      "type": "array",
      "title": "Dependencies",
      "default": []
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "kind",
     "stable_key",
     "scope",
     "title"
    ],
    "title": "LibraryResourceCreate",
    "description": "Creates the resource row plus its draft version 1 (which never moves\n`active_version` by itself \u2014 activation is a separate explicit operation,\n`project_id` is required for project scope and\nforbidden otherwise; `owner_user_id` is always server-derived, never\nclient-supplied."
   },
   "LibraryScope": {
    "type": "string",
    "enum": [
     "studio",
     "project",
     "user"
    ],
    "title": "LibraryScope"
   },
   "LibraryStatus": {
    "type": "string",
    "enum": [
     "draft",
     "active",
     "deprecated"
    ],
    "title": "LibraryStatus"
   },
   "LibraryVersion": {
    "properties": {
     "id": {
      "type": "string",
      "format": "uuid",
      "title": "Id"
     },
     "resource_id": {
      "type": "string",
      "format": "uuid",
      "title": "Resource Id"
     },
     "version": {
      "type": "integer",
      "title": "Version"
     },
     "title": {
      "type": "string",
      "title": "Title"
     },
     "description": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Description"
     },
     "content": {
      "additionalProperties": true,
      "type": "object",
      "title": "Content",
      "default": {}
     },
     "dependencies": {
      "items": {
       "$ref": "#/components/schemas/DependencyPin"
      },
      "type": "array",
      "title": "Dependencies",
      "default": []
     },
     "created_by_user_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Created By User Id"
     },
     "created_at": {
      "type": "string",
      "format": "date-time",
      "title": "Created At"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "id",
     "resource_id",
     "version",
     "title",
     "created_at"
    ],
    "title": "LibraryVersion",
    "description": "One immutable snapshot of a resource version."
   },
   "LibraryVersionCreate": {
    "properties": {
     "title": {
      "type": "string",
      "minLength": 1,
      "title": "Title"
     },
     "description": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Description"
     },
     "content": {
      "additionalProperties": true,
      "type": "object",
      "title": "Content",
      "default": {}
     },
     "dependencies": {
      "items": {
       "$ref": "#/components/schemas/DependencyPin"
      },
      "type": "array",
      "title": "Dependencies",
      "default": []
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "title"
    ],
    "title": "LibraryVersionCreate"
   },
   "Machine": {
    "properties": {
     "created_at": {
      "type": "string",
      "format": "date-time",
      "title": "Created At"
     },
     "updated_at": {
      "type": "string",
      "format": "date-time",
      "title": "Updated At"
     },
     "version": {
      "type": "integer",
      "title": "Version"
     },
     "id": {
      "type": "string",
      "format": "uuid",
      "title": "Id"
     },
     "owner_user_id": {
      "type": "string",
      "format": "uuid",
      "title": "Owner User Id"
     },
     "display_name": {
      "type": "string",
      "title": "Display Name"
     },
     "last_seen_at": {
      "anyOf": [
       {
        "type": "string",
        "format": "date-time"
       },
       {
        "type": "null"
       }
      ],
      "title": "Last Seen At"
     },
     "status": {
      "$ref": "#/components/schemas/MachineStatus",
      "default": "offline"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "created_at",
     "updated_at",
     "version",
     "id",
     "owner_user_id",
     "display_name"
    ],
    "title": "Machine",
    "description": "A machine owns its own revocable credential: an opaque token whose\nhash alone is stored server-side, so a leaked database never leaks\naccess."
   },
   "MachineCreate": {
    "properties": {
     "owner_user_id": {
      "type": "string",
      "format": "uuid",
      "title": "Owner User Id"
     },
     "display_name": {
      "type": "string",
      "title": "Display Name"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "owner_user_id",
     "display_name"
    ],
    "title": "MachineCreate"
   },
   "MachineCreated": {
    "properties": {
     "created_at": {
      "type": "string",
      "format": "date-time",
      "title": "Created At"
     },
     "updated_at": {
      "type": "string",
      "format": "date-time",
      "title": "Updated At"
     },
     "version": {
      "type": "integer",
      "title": "Version"
     },
     "id": {
      "type": "string",
      "format": "uuid",
      "title": "Id"
     },
     "owner_user_id": {
      "type": "string",
      "format": "uuid",
      "title": "Owner User Id"
     },
     "display_name": {
      "type": "string",
      "title": "Display Name"
     },
     "last_seen_at": {
      "anyOf": [
       {
        "type": "string",
        "format": "date-time"
       },
       {
        "type": "null"
       }
      ],
      "title": "Last Seen At"
     },
     "status": {
      "$ref": "#/components/schemas/MachineStatus",
      "default": "offline"
     },
     "credential": {
      "type": "string",
      "title": "Credential"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "created_at",
     "updated_at",
     "version",
     "id",
     "owner_user_id",
     "display_name",
     "credential"
    ],
    "title": "MachineCreated",
    "description": "Returned once, at creation time: the opaque credential in clear text.\nNever retrievable again afterwards \u2014 only its hash is stored."
   },
   "MachineStatus": {
    "type": "string",
    "enum": [
     "online",
     "idle",
     "offline"
    ],
    "title": "MachineStatus"
   },
   "PreservedReference": {
    "properties": {
     "resource_id": {
      "type": "string",
      "format": "uuid",
      "title": "Resource Id"
     },
     "kind": {
      "$ref": "#/components/schemas/LibraryKind"
     },
     "stable_key": {
      "type": "string",
      "title": "Stable Key"
     },
     "scope": {
      "$ref": "#/components/schemas/LibraryScope"
     },
     "version": {
      "type": "integer",
      "title": "Version"
     },
     "version_origin": {
      "$ref": "#/components/schemas/VersionOrigin"
     },
     "deprecated": {
      "type": "boolean",
      "title": "Deprecated",
      "default": false
     },
     "relation": {
      "$ref": "#/components/schemas/BindingRelation"
     },
     "provenance": {
      "$ref": "#/components/schemas/Provenance"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "resource_id",
     "kind",
     "stable_key",
     "scope",
     "version",
     "version_origin",
     "relation",
     "provenance"
    ],
    "title": "PreservedReference",
    "description": "A `composes_agent` / `references_workflow` dependency, preserved with\nidentity, exact version and provenance \u2014 never expanded: workflow\nexecution semantics belong to P11 and agent-composition execution has no\ndefined semantics yet (library bindings, resolution engine)."
   },
   "ProducerJob": {
    "properties": {
     "id": {
      "type": "string",
      "format": "uuid",
      "title": "Id"
     },
     "project_id": {
      "type": "string",
      "format": "uuid",
      "title": "Project Id"
     },
     "kind": {
      "$ref": "#/components/schemas/ProducerJobKind"
     },
     "status": {
      "$ref": "#/components/schemas/ProducerJobStatus",
      "default": "requested"
     },
     "task_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Task Id"
     },
     "result": {
      "additionalProperties": true,
      "type": "object",
      "title": "Result",
      "default": {}
     },
     "error": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Error"
     },
     "created_at": {
      "type": "string",
      "format": "date-time",
      "title": "Created At"
     },
     "completed_at": {
      "anyOf": [
       {
        "type": "string",
        "format": "date-time"
       },
       {
        "type": "null"
       }
      ],
      "title": "Completed At"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "id",
     "project_id",
     "kind",
     "created_at"
    ],
    "title": "ProducerJob",
    "description": "A bounded, synchronous Producer computation over one project's shared\nstate. The Producer never mutates `Task`/`ResourceClaim`:\na `decomposition` result is a proposal \u2014 the caller creates the\nsub-tasks itself via `POST /tasks` (idempotent)."
   },
   "ProducerJobKind": {
    "type": "string",
    "enum": [
     "priority_analysis",
     "blocker_detection",
     "parallelization",
     "decomposition"
    ],
    "title": "ProducerJobKind",
    "description": "What the Producer was asked to compute. Deterministic and\nmodel-agnostic in v1 \u2014 no LLM in the server core."
   },
   "ProducerJobRequest": {
    "properties": {
     "project_id": {
      "type": "string",
      "format": "uuid",
      "title": "Project Id"
     },
     "kind": {
      "$ref": "#/components/schemas/ProducerJobKind"
     },
     "task_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Task Id"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "project_id",
     "kind"
    ],
    "title": "ProducerJobRequest"
   },
   "ProducerJobStatus": {
    "type": "string",
    "enum": [
     "requested",
     "running",
     "completed",
     "failed"
    ],
    "title": "ProducerJobStatus"
   },
   "Project": {
    "properties": {
     "created_at": {
      "type": "string",
      "format": "date-time",
      "title": "Created At"
     },
     "updated_at": {
      "type": "string",
      "format": "date-time",
      "title": "Updated At"
     },
     "version": {
      "type": "integer",
      "title": "Version"
     },
     "id": {
      "type": "string",
      "format": "uuid",
      "title": "Id"
     },
     "slug": {
      "type": "string",
      "title": "Slug"
     },
     "name": {
      "type": "string",
      "title": "Name"
     },
     "description": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Description"
     },
     "archived": {
      "type": "boolean",
      "title": "Archived",
      "default": false
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "created_at",
     "updated_at",
     "version",
     "id",
     "slug",
     "name"
    ],
    "title": "Project"
   },
   "ProjectCreate": {
    "properties": {
     "slug": {
      "type": "string",
      "title": "Slug"
     },
     "name": {
      "type": "string",
      "title": "Name"
     },
     "description": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Description"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "slug",
     "name"
    ],
    "title": "ProjectCreate"
   },
   "ProjectState": {
    "properties": {
     "project_id": {
      "type": "string",
      "format": "uuid",
      "title": "Project Id"
     },
     "active_tasks": {
      "items": {
       "$ref": "#/components/schemas/Task"
      },
      "type": "array",
      "title": "Active Tasks"
     },
     "active_claims": {
      "items": {
       "$ref": "#/components/schemas/ResourceClaim"
      },
      "type": "array",
      "title": "Active Claims"
     },
     "generated_at": {
      "type": "string",
      "format": "date-time",
      "title": "Generated At"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "project_id",
     "active_tasks",
     "active_claims",
     "generated_at"
    ],
    "title": "ProjectState",
    "description": "GET /projects/{id}/state \u2014 the aggregate view a client bootstraps from."
   },
   "Provenance": {
    "properties": {
     "source": {
      "$ref": "#/components/schemas/ProvenanceSource"
     },
     "resource_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Resource Id"
     },
     "stable_key": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Stable Key"
     },
     "scope": {
      "anyOf": [
       {
        "$ref": "#/components/schemas/LibraryScope"
       },
       {
        "type": "null"
       }
      ]
     },
     "version": {
      "anyOf": [
       {
        "type": "integer"
       },
       {
        "type": "null"
       }
      ],
      "title": "Version"
     },
     "version_origin": {
      "anyOf": [
       {
        "$ref": "#/components/schemas/VersionOrigin"
       },
       {
        "type": "null"
       }
      ]
     },
     "locked": {
      "type": "boolean",
      "title": "Locked",
      "default": false
     },
     "relation": {
      "anyOf": [
       {
        "$ref": "#/components/schemas/BindingRelation"
       },
       {
        "type": "null"
       }
      ]
     },
     "binding_level": {
      "anyOf": [
       {
        "$ref": "#/components/schemas/RuntimeLevel"
       },
       {
        "type": "null"
       }
      ]
     },
     "via": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Via"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "source"
    ],
    "title": "Provenance",
    "description": "Complete provenance for one resolved element.\n\nAnswers: why this resource, why this version (lock vs active vs pin),\nthrough which relation, which runtime level won and whether an override\ndecided. Every field is data, never a human sentence."
   },
   "ProvenanceSource": {
    "type": "string",
    "enum": [
     "active_pointer",
     "project_lock",
     "version_pin",
     "runtime_binding",
     "session_override"
    ],
    "title": "ProvenanceSource",
    "description": "Where a resolved element came from (structured, never prose)."
   },
   "ResolvedAgent": {
    "properties": {
     "resource_id": {
      "type": "string",
      "format": "uuid",
      "title": "Resource Id"
     },
     "kind": {
      "$ref": "#/components/schemas/LibraryKind"
     },
     "stable_key": {
      "type": "string",
      "title": "Stable Key"
     },
     "scope": {
      "$ref": "#/components/schemas/LibraryScope"
     },
     "version": {
      "type": "integer",
      "title": "Version"
     },
     "version_origin": {
      "$ref": "#/components/schemas/VersionOrigin"
     },
     "deprecated": {
      "type": "boolean",
      "title": "Deprecated",
      "default": false
     },
     "title": {
      "type": "string",
      "title": "Title",
      "default": ""
     },
     "content": {
      "additionalProperties": true,
      "type": "object",
      "title": "Content",
      "default": {}
     },
     "provenance": {
      "$ref": "#/components/schemas/Provenance"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "resource_id",
     "kind",
     "stable_key",
     "scope",
     "version",
     "version_origin",
     "provenance"
    ],
    "title": "ResolvedAgent"
   },
   "ResolvedAgentDefinition": {
    "properties": {
     "agent": {
      "$ref": "#/components/schemas/ResolvedAgent"
     },
     "rules": {
      "items": {
       "$ref": "#/components/schemas/ResolvedRule"
      },
      "type": "array",
      "title": "Rules",
      "default": []
     },
     "skills": {
      "items": {
       "$ref": "#/components/schemas/ResolvedSkill"
      },
      "type": "array",
      "title": "Skills",
      "default": []
     },
     "model_profile": {
      "anyOf": [
       {
        "$ref": "#/components/schemas/ResolvedModelProfile"
       },
       {
        "type": "null"
       }
      ]
     },
     "requirements": {
      "$ref": "#/components/schemas/CapabilityRequirement",
      "default": {
       "coding": false,
       "tools_required": [],
       "local_compatible": false
      }
     },
     "composed_agents": {
      "items": {
       "$ref": "#/components/schemas/PreservedReference"
      },
      "type": "array",
      "title": "Composed Agents",
      "default": []
     },
     "workflows": {
      "items": {
       "$ref": "#/components/schemas/PreservedReference"
      },
      "type": "array",
      "title": "Workflows",
      "default": []
     },
     "runtime": {
      "anyOf": [
       {
        "$ref": "#/components/schemas/ResolvedRuntime"
       },
       {
        "type": "null"
       }
      ]
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "agent"
    ],
    "title": "ResolvedAgentDefinition",
    "description": "Canonical P5 output: the complete logical agent specification.\n\nHarness-neutral (no harness-specific branch) and\nprovider-neutral (`provider_ref`/`model_ref` travel as opaque strings\nwhen a binding carries them; no vendor catalog lives here). Stable input\nfor P7 HTTP, P8 MCP, P9 Context Package and P10 adapters."
   },
   "ResolvedModelProfile": {
    "properties": {
     "resource_id": {
      "type": "string",
      "format": "uuid",
      "title": "Resource Id"
     },
     "stable_key": {
      "type": "string",
      "title": "Stable Key"
     },
     "scope": {
      "$ref": "#/components/schemas/LibraryScope"
     },
     "version": {
      "type": "integer",
      "title": "Version"
     },
     "version_origin": {
      "$ref": "#/components/schemas/VersionOrigin"
     },
     "deprecated": {
      "type": "boolean",
      "title": "Deprecated",
      "default": false
     },
     "title": {
      "type": "string",
      "title": "Title",
      "default": ""
     },
     "requirements": {
      "$ref": "#/components/schemas/CapabilityRequirement",
      "default": {
       "coding": false,
       "tools_required": [],
       "local_compatible": false
      }
     },
     "provenance": {
      "$ref": "#/components/schemas/Provenance"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "resource_id",
     "stable_key",
     "scope",
     "version",
     "version_origin",
     "provenance"
    ],
    "title": "ResolvedModelProfile"
   },
   "ResolvedRule": {
    "properties": {
     "resource_id": {
      "type": "string",
      "format": "uuid",
      "title": "Resource Id"
     },
     "stable_key": {
      "type": "string",
      "title": "Stable Key"
     },
     "scope": {
      "$ref": "#/components/schemas/LibraryScope"
     },
     "version": {
      "type": "integer",
      "title": "Version"
     },
     "version_origin": {
      "$ref": "#/components/schemas/VersionOrigin"
     },
     "deprecated": {
      "type": "boolean",
      "title": "Deprecated",
      "default": false
     },
     "title": {
      "type": "string",
      "title": "Title",
      "default": ""
     },
     "content": {
      "additionalProperties": true,
      "type": "object",
      "title": "Content",
      "default": {}
     },
     "paths": {
      "items": {
       "$ref": "#/components/schemas/RulePath"
      },
      "type": "array",
      "title": "Paths",
      "default": []
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "resource_id",
     "stable_key",
     "scope",
     "version",
     "version_origin"
    ],
    "title": "ResolvedRule"
   },
   "ResolvedRuntime": {
    "properties": {
     "target": {
      "$ref": "#/components/schemas/RuntimeTarget"
     },
     "level": {
      "$ref": "#/components/schemas/RuntimeLevel"
     },
     "matched_kind": {
      "$ref": "#/components/schemas/LibraryKind"
     },
     "matched_stable_key": {
      "type": "string",
      "title": "Matched Stable Key"
     },
     "compatible": {
      "type": "boolean",
      "title": "Compatible",
      "default": true
     },
     "unsatisfied": {
      "items": {
       "type": "string"
      },
      "type": "array",
      "title": "Unsatisfied",
      "default": []
     },
     "provenance": {
      "anyOf": [
       {
        "$ref": "#/components/schemas/Provenance"
       },
       {
        "type": "null"
       }
      ]
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "target",
     "level",
     "matched_kind",
     "matched_stable_key"
    ],
    "title": "ResolvedRuntime",
    "description": "The winning runtime choice. `compatible` is always `True` here: an\nexplicitly selected but incompatible target is a `runtime_incompatible`\nerror, never a silent fallback and never a silent `null`."
   },
   "ResolvedSkill": {
    "properties": {
     "resource_id": {
      "type": "string",
      "format": "uuid",
      "title": "Resource Id"
     },
     "stable_key": {
      "type": "string",
      "title": "Stable Key"
     },
     "scope": {
      "$ref": "#/components/schemas/LibraryScope"
     },
     "version": {
      "type": "integer",
      "title": "Version"
     },
     "version_origin": {
      "$ref": "#/components/schemas/VersionOrigin"
     },
     "deprecated": {
      "type": "boolean",
      "title": "Deprecated",
      "default": false
     },
     "title": {
      "type": "string",
      "title": "Title",
      "default": ""
     },
     "content": {
      "additionalProperties": true,
      "type": "object",
      "title": "Content",
      "default": {}
     },
     "provenance": {
      "$ref": "#/components/schemas/Provenance"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "resource_id",
     "stable_key",
     "scope",
     "version",
     "version_origin",
     "provenance"
    ],
    "title": "ResolvedSkill"
   },
   "ResourceClaim": {
    "properties": {
     "id": {
      "type": "string",
      "format": "uuid",
      "title": "Id"
     },
     "project_id": {
      "type": "string",
      "format": "uuid",
      "title": "Project Id"
     },
     "task_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Task Id"
     },
     "resource_path": {
      "type": "string",
      "title": "Resource Path"
     },
     "resource_type": {
      "$ref": "#/components/schemas/ResourceType"
     },
     "claimed_by_machine_id": {
      "type": "string",
      "format": "uuid",
      "title": "Claimed By Machine Id"
     },
     "claimed_by_agent_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Claimed By Agent Id"
     },
     "status": {
      "$ref": "#/components/schemas/ClaimStatus",
      "default": "active"
     },
     "ttl_seconds": {
      "type": "integer",
      "title": "Ttl Seconds"
     },
     "created_at": {
      "type": "string",
      "format": "date-time",
      "title": "Created At"
     },
     "renewed_at": {
      "anyOf": [
       {
        "type": "string",
        "format": "date-time"
       },
       {
        "type": "null"
       }
      ],
      "title": "Renewed At"
     },
     "expires_at": {
      "type": "string",
      "format": "date-time",
      "title": "Expires At"
     },
     "released_at": {
      "anyOf": [
       {
        "type": "string",
        "format": "date-time"
       },
       {
        "type": "null"
       }
      ],
      "title": "Released At"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "id",
     "project_id",
     "resource_path",
     "resource_type",
     "claimed_by_machine_id",
     "ttl_seconds",
     "created_at",
     "expires_at"
    ],
    "title": "ResourceClaim"
   },
   "ResourceClaimCreate": {
    "properties": {
     "project_id": {
      "type": "string",
      "format": "uuid",
      "title": "Project Id"
     },
     "task_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Task Id"
     },
     "resource_path": {
      "type": "string",
      "title": "Resource Path"
     },
     "resource_type": {
      "$ref": "#/components/schemas/ResourceType"
     },
     "ttl_seconds": {
      "type": "integer",
      "title": "Ttl Seconds"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "project_id",
     "resource_path",
     "resource_type",
     "ttl_seconds"
    ],
    "title": "ResourceClaimCreate"
   },
   "ResourceType": {
    "type": "string",
    "enum": [
     "file",
     "folder"
    ],
    "title": "ResourceType"
   },
   "ReviewQueue": {
    "properties": {
     "items": {
      "items": {
       "oneOf": [
        {
         "$ref": "#/components/schemas/ReviewQueueAIWorkItem"
        },
        {
         "$ref": "#/components/schemas/ReviewQueueDecisionItem"
        },
        {
         "$ref": "#/components/schemas/ReviewQueueConflictItem"
        },
        {
         "$ref": "#/components/schemas/ReviewQueueBuildItem"
        },
        {
         "$ref": "#/components/schemas/ReviewQueuePRItem"
        }
       ],
       "discriminator": {
        "propertyName": "kind",
        "mapping": {
         "ai_work_review": "#/components/schemas/ReviewQueueAIWorkItem",
         "build_failure": "#/components/schemas/ReviewQueueBuildItem",
         "decision_proposal": "#/components/schemas/ReviewQueueDecisionItem",
         "pr_ready": "#/components/schemas/ReviewQueuePRItem",
         "resource_conflict": "#/components/schemas/ReviewQueueConflictItem"
        }
       }
      },
      "type": "array",
      "title": "Items"
     },
     "generated_at": {
      "type": "string",
      "format": "date-time",
      "title": "Generated At"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "items",
     "generated_at"
    ],
    "title": "ReviewQueue",
    "description": "Items sorted by `requested_at` descending (newest first)."
   },
   "ReviewQueueAIWorkItem": {
    "properties": {
     "kind": {
      "type": "string",
      "const": "ai_work_review",
      "title": "Kind",
      "default": "ai_work_review"
     },
     "id": {
      "type": "string",
      "format": "uuid",
      "title": "Id"
     },
     "project_id": {
      "type": "string",
      "format": "uuid",
      "title": "Project Id"
     },
     "task_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Task Id"
     },
     "title": {
      "type": "string",
      "title": "Title"
     },
     "agent_id": {
      "type": "string",
      "format": "uuid",
      "title": "Agent Id"
     },
     "requested_at": {
      "type": "string",
      "format": "date-time",
      "title": "Requested At"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "id",
     "project_id",
     "title",
     "agent_id",
     "requested_at"
    ],
    "title": "ReviewQueueAIWorkItem"
   },
   "ReviewQueueBuildItem": {
    "properties": {
     "kind": {
      "type": "string",
      "const": "build_failure",
      "title": "Kind",
      "default": "build_failure"
     },
     "id": {
      "type": "string",
      "format": "uuid",
      "title": "Id"
     },
     "project_id": {
      "type": "string",
      "format": "uuid",
      "title": "Project Id"
     },
     "task_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Task Id"
     },
     "title": {
      "type": "string",
      "title": "Title"
     },
     "workflow_name": {
      "type": "string",
      "title": "Workflow Name"
     },
     "branch": {
      "type": "string",
      "title": "Branch"
     },
     "commit_sha": {
      "type": "string",
      "title": "Commit Sha"
     },
     "conclusion": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Conclusion"
     },
     "requested_at": {
      "type": "string",
      "format": "date-time",
      "title": "Requested At"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "id",
     "project_id",
     "title",
     "workflow_name",
     "branch",
     "commit_sha",
     "requested_at"
    ],
    "title": "ReviewQueueBuildItem",
    "description": "`id` is the failed `Build`'s id \u2014 informational (no build transition\nendpoint exists), like `ReviewQueueConflictItem`. `requested_at` is when\nthe build completed as failed."
   },
   "ReviewQueueConflictItem": {
    "properties": {
     "kind": {
      "type": "string",
      "const": "resource_conflict",
      "title": "Kind",
      "default": "resource_conflict"
     },
     "id": {
      "type": "string",
      "format": "uuid",
      "title": "Id"
     },
     "project_id": {
      "type": "string",
      "format": "uuid",
      "title": "Project Id"
     },
     "task_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Task Id"
     },
     "title": {
      "type": "string",
      "title": "Title"
     },
     "resource_path": {
      "type": "string",
      "title": "Resource Path"
     },
     "requested_at": {
      "type": "string",
      "format": "date-time",
      "title": "Requested At"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "id",
     "project_id",
     "title",
     "resource_path",
     "requested_at"
    ],
    "title": "ReviewQueueConflictItem",
    "description": "`id` is the `resource.conflict` event's `event_id` \u2014 not a persisted\nconflict row (none exists): a best-effort, time-windowed\nsignal, not a resolvable state."
   },
   "ReviewQueueDecisionItem": {
    "properties": {
     "kind": {
      "type": "string",
      "const": "decision_proposal",
      "title": "Kind",
      "default": "decision_proposal"
     },
     "id": {
      "type": "string",
      "format": "uuid",
      "title": "Id"
     },
     "project_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Project Id"
     },
     "task_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Task Id"
     },
     "readable_id": {
      "type": "string",
      "title": "Readable Id"
     },
     "title": {
      "type": "string",
      "title": "Title"
     },
     "proposed_by_type": {
      "type": "string",
      "title": "Proposed By Type"
     },
     "requested_at": {
      "type": "string",
      "format": "date-time",
      "title": "Requested At"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "id",
     "readable_id",
     "title",
     "proposed_by_type",
     "requested_at"
    ],
    "title": "ReviewQueueDecisionItem"
   },
   "ReviewQueuePRItem": {
    "properties": {
     "kind": {
      "type": "string",
      "const": "pr_ready",
      "title": "Kind",
      "default": "pr_ready"
     },
     "id": {
      "type": "string",
      "format": "uuid",
      "title": "Id"
     },
     "project_id": {
      "type": "string",
      "format": "uuid",
      "title": "Project Id"
     },
     "task_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Task Id"
     },
     "title": {
      "type": "string",
      "title": "Title"
     },
     "pr_number": {
      "type": "integer",
      "title": "Pr Number"
     },
     "head_branch": {
      "type": "string",
      "title": "Head Branch"
     },
     "base_branch": {
      "type": "string",
      "title": "Base Branch"
     },
     "requested_at": {
      "type": "string",
      "format": "date-time",
      "title": "Requested At"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "id",
     "project_id",
     "title",
     "pr_number",
     "head_branch",
     "base_branch",
     "requested_at"
    ],
    "title": "ReviewQueuePRItem",
    "description": "`id` is the `git.pr.opened` event's `event_id` \u2014 a best-effort,\ntime-windowed signal (a PR opened long ago with no `git.pr.merged` ages\nout of the window), not a resolvable state."
   },
   "Role": {
    "type": "string",
    "enum": [
     "admin",
     "developer",
     "agent",
     "readonly"
    ],
    "title": "Role",
    "description": "Account roles, weakest to strongest: `readonly` (reads plus heartbeat\nonly, no business writes); `agent` (writes, but never project, machine\nor user provisioning); `developer` (writes, plus project creation);\n`admin` (everything, including machine/user provisioning and work\nreview resolution). The role always belongs to the machine owner's\nuser, never to a harness, provider, model or profile."
   },
   "RulePath": {
    "properties": {
     "relation": {
      "$ref": "#/components/schemas/BindingRelation"
     },
     "via_kind": {
      "$ref": "#/components/schemas/LibraryKind"
     },
     "via_resource_id": {
      "type": "string",
      "format": "uuid",
      "title": "Via Resource Id"
     },
     "via_stable_key": {
      "type": "string",
      "title": "Via Stable Key"
     },
     "via_version": {
      "type": "integer",
      "title": "Via Version"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "relation",
     "via_kind",
     "via_resource_id",
     "via_stable_key",
     "via_version"
    ],
    "title": "RulePath",
    "description": "One provenance path that produced a resolved rule.\n\nThe same rule reached through several paths yields one logical object\nwith one `RulePath` per path \u2014 deduplicated content, preserved history."
   },
   "RuntimeBinding": {
    "properties": {
     "id": {
      "type": "string",
      "format": "uuid",
      "title": "Id"
     },
     "level": {
      "$ref": "#/components/schemas/RuntimeLevel"
     },
     "owner_user_id": {
      "type": "string",
      "format": "uuid",
      "title": "Owner User Id"
     },
     "project_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Project Id"
     },
     "target_kind": {
      "$ref": "#/components/schemas/LibraryKind"
     },
     "target_stable_key": {
      "type": "string",
      "title": "Target Stable Key"
     },
     "target": {
      "$ref": "#/components/schemas/RuntimeTarget"
     },
     "created_at": {
      "type": "string",
      "format": "date-time",
      "title": "Created At"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "id",
     "level",
     "owner_user_id",
     "target_kind",
     "target_stable_key",
     "target",
     "created_at"
    ],
    "title": "RuntimeBinding",
    "description": "One stored runtime choice for a logical `(kind, stable_key)` key.\n\nThe key is logical (never a version pin): the preference follows the\ndefinition across versions. `owner_user_id` is always the creating user\n(server-derived, never client-supplied): for `user` bindings it is the\nbeneficiary, for shared levels it is the creator (release rules mirror\nproject locks)."
   },
   "RuntimeBindingCreate": {
    "properties": {
     "level": {
      "$ref": "#/components/schemas/RuntimeLevel"
     },
     "project_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Project Id"
     },
     "target_kind": {
      "$ref": "#/components/schemas/LibraryKind"
     },
     "target_stable_key": {
      "type": "string",
      "maxLength": 200,
      "minLength": 1,
      "title": "Target Stable Key"
     },
     "target": {
      "$ref": "#/components/schemas/RuntimeTarget"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "level",
     "target_kind",
     "target_stable_key",
     "target"
    ],
    "title": "RuntimeBindingCreate",
    "description": "Creates one stored runtime choice. `owner_user_id` is never accepted:\nthe server derives it from the authenticated principal."
   },
   "RuntimeCapabilities": {
    "properties": {
     "reasoning": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Reasoning"
     },
     "coding": {
      "type": "boolean",
      "title": "Coding",
      "default": false
     },
     "context_window": {
      "anyOf": [
       {
        "type": "integer"
       },
       {
        "type": "null"
       }
      ],
      "title": "Context Window"
     },
     "tools": {
      "items": {
       "type": "string"
      },
      "type": "array",
      "title": "Tools",
      "default": []
     },
     "multimodal": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Multimodal"
     },
     "local": {
      "type": "boolean",
      "title": "Local",
      "default": false
     },
     "cost": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Cost"
     },
     "latency": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Latency"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "title": "RuntimeCapabilities",
    "description": "Abstract capability surface the P6 Runtime Registry will satisfy.\nOpen strings, same vocabulary as `CapabilityRequirement`, never a vendor\ncatalog."
   },
   "RuntimeLevel": {
    "type": "string",
    "enum": [
     "session",
     "project_override",
     "user",
     "project_default",
     "studio_default"
    ],
    "title": "RuntimeLevel",
    "description": "Stored and ephemeral levels of runtime choice, strongest first.\n\n`session` is never persisted \u2014 it travels with the resolve call only.\n`project_override` beats a personal choice (explicit project pinning);\n`project_default` and `studio_default` only apply below it."
   },
   "RuntimeRegistration": {
    "properties": {
     "id": {
      "type": "string",
      "format": "uuid",
      "title": "Id"
     },
     "owner_user_id": {
      "type": "string",
      "format": "uuid",
      "title": "Owner User Id"
     },
     "machine_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Machine Id"
     },
     "harness_ref": {
      "anyOf": [
       {
        "type": "string",
        "maxLength": 200,
        "minLength": 1
       },
       {
        "type": "null"
       }
      ],
      "title": "Harness Ref"
     },
     "provider_ref": {
      "anyOf": [
       {
        "type": "string",
        "maxLength": 200,
        "minLength": 1
       },
       {
        "type": "null"
       }
      ],
      "title": "Provider Ref"
     },
     "model_ref": {
      "anyOf": [
       {
        "type": "string",
        "maxLength": 200,
        "minLength": 1
       },
       {
        "type": "null"
       }
      ],
      "title": "Model Ref"
     },
     "capabilities": {
      "$ref": "#/components/schemas/RuntimeCapabilities",
      "default": {
       "coding": false,
       "tools": [],
       "local": false
      }
     },
     "capability_source": {
      "$ref": "#/components/schemas/CapabilitySource",
      "default": "declared"
     },
     "runtime_metadata": {
      "additionalProperties": true,
      "type": "object",
      "title": "Runtime Metadata",
      "default": {}
     },
     "status": {
      "$ref": "#/components/schemas/RuntimeStatus",
      "default": "active"
     },
     "version": {
      "type": "integer",
      "title": "Version",
      "description": "Optimistic-concurrency revision (P7): mirrors the server-side revision column. Additive P7 exposure \u2014 previously only visible server-side; readers need it to send `expected_version` on update instead of guessing. Defaults to 1 (the DB default) so non-persisted constructions stay valid.",
      "default": 1
     },
     "created_at": {
      "type": "string",
      "format": "date-time",
      "title": "Created At"
     },
     "updated_at": {
      "anyOf": [
       {
        "type": "string",
        "format": "date-time"
       },
       {
        "type": "null"
       }
      ],
      "title": "Updated At"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "id",
     "owner_user_id",
     "created_at"
    ],
    "title": "RuntimeRegistration",
    "description": "Canonical persisted description of one declared runtime (P6).\n\nHarness, provider, model and runtime are four different concepts, stored\nside by side but never fused: `harness_ref` names the consuming\nsoftware, `provider_ref` the system exposing models, `model_ref` the\nconcrete model, and the row identity (`id`, stable UUID) names the\nruntime itself. All refs are open strings. `owner_user_id` is always\nserver-derived. `machine_id` is optional: local/attached runtimes point\nat an owned machine, remote/cloud runtimes leave it null \u2014 one\nabstraction, no per-locus architecture. `capabilities` are declared\n(`capability_source`) and feed the pure `check_compatibility` matcher;\n`unknown != compatible` still holds \u2014 a model name never proves a\ncapability. No secret field exists by construction."
   },
   "RuntimeRegistrationCreate": {
    "properties": {
     "machine_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Machine Id"
     },
     "harness_ref": {
      "anyOf": [
       {
        "type": "string",
        "maxLength": 200,
        "minLength": 1
       },
       {
        "type": "null"
       }
      ],
      "title": "Harness Ref"
     },
     "provider_ref": {
      "anyOf": [
       {
        "type": "string",
        "maxLength": 200,
        "minLength": 1
       },
       {
        "type": "null"
       }
      ],
      "title": "Provider Ref"
     },
     "model_ref": {
      "anyOf": [
       {
        "type": "string",
        "maxLength": 200,
        "minLength": 1
       },
       {
        "type": "null"
       }
      ],
      "title": "Model Ref"
     },
     "capabilities": {
      "$ref": "#/components/schemas/RuntimeCapabilities",
      "default": {
       "coding": false,
       "tools": [],
       "local": false
      }
     },
     "capability_source": {
      "$ref": "#/components/schemas/CapabilitySource",
      "default": "declared"
     },
     "runtime_metadata": {
      "additionalProperties": true,
      "type": "object",
      "title": "Runtime Metadata",
      "default": {}
     }
    },
    "additionalProperties": false,
    "type": "object",
    "title": "RuntimeRegistrationCreate",
    "description": "Registers one runtime. `owner_user_id` is never accepted (server\nderives it from the authenticated principal); `status` is always\n`active` at creation (revocation is a separate explicit operation).\nOnly `declared` capabilities exist in P6 \u2014 any other source is\nfail-closed until defined."
   },
   "RuntimeRegistrationUpdate": {
    "properties": {
     "machine_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Machine Id"
     },
     "detach_machine": {
      "type": "boolean",
      "title": "Detach Machine",
      "default": false
     },
     "harness_ref": {
      "anyOf": [
       {
        "type": "string",
        "maxLength": 200,
        "minLength": 1
       },
       {
        "type": "null"
       }
      ],
      "title": "Harness Ref"
     },
     "provider_ref": {
      "anyOf": [
       {
        "type": "string",
        "maxLength": 200,
        "minLength": 1
       },
       {
        "type": "null"
       }
      ],
      "title": "Provider Ref"
     },
     "model_ref": {
      "anyOf": [
       {
        "type": "string",
        "maxLength": 200,
        "minLength": 1
       },
       {
        "type": "null"
       }
      ],
      "title": "Model Ref"
     },
     "capabilities": {
      "anyOf": [
       {
        "$ref": "#/components/schemas/RuntimeCapabilities"
       },
       {
        "type": "null"
       }
      ]
     },
     "runtime_metadata": {
      "anyOf": [
       {
        "additionalProperties": true,
        "type": "object"
       },
       {
        "type": "null"
       }
      ],
      "title": "Runtime Metadata"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "title": "RuntimeRegistrationUpdate",
    "description": "Mutates the descriptors of one runtime without rotating its identity:\nchanging capabilities (or refs) never creates a new runtime \u2014 `id` is\nthe stable     identity, refs are mutable descriptors. Every field is\noptional; omitted fields are left untouched. `machine_id` can be\nattached or moved (a value sets a new locus); detaching uses the\nexplicit `detach_machine` flag \u2014 an omitted `machine_id` (`None`)\nmeans \"leave untouched\", never \"detach\" \u2014 because the model cannot\ndistinguish omitted from explicit `null`. The target machine must\nstill be owned by the caller. `expected_version` (service call) guards\nagainst stale writes (409)."
   },
   "RuntimeRegistrationUpdateRequest": {
    "properties": {
     "update": {
      "$ref": "#/components/schemas/RuntimeRegistrationUpdate"
     },
     "expected_version": {
      "type": "integer",
      "minimum": 1.0,
      "title": "Expected Version"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "update",
     "expected_version"
    ],
    "title": "RuntimeRegistrationUpdateRequest",
    "description": "HTTP/MCP input for a registry update (P7): the descriptor\npatch plus the optimistic-concurrency guard. Nested (not flattened) so\nthe patch shape stays exactly `RuntimeRegistrationUpdate` \u2014 one\ndefinition for HTTP, MCP and service, never a divergent copy. The\nrouter forwards `update` and `expected_version` unchanged to\n`update_runtime`."
   },
   "RuntimeStatus": {
    "type": "string",
    "enum": [
     "active",
     "revoked"
    ],
    "title": "RuntimeStatus",
    "description": "Registry lifecycle (P6): logical revocation only, never a\nphysical delete \u2014 a revoked runtime stays readable (diagnostics) but\nresolves as non-live, so a binding toward it falls through instead of\nsilently becoming another target."
   },
   "RuntimeTarget": {
    "properties": {
     "runtime_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Runtime Id"
     },
     "machine_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Machine Id"
     },
     "harness_ref": {
      "anyOf": [
       {
        "type": "string",
        "maxLength": 200,
        "minLength": 1
       },
       {
        "type": "null"
       }
      ],
      "title": "Harness Ref"
     },
     "provider_ref": {
      "anyOf": [
       {
        "type": "string",
        "maxLength": 200,
        "minLength": 1
       },
       {
        "type": "null"
       }
      ],
      "title": "Provider Ref"
     },
     "model_ref": {
      "anyOf": [
       {
        "type": "string",
        "maxLength": 200,
        "minLength": 1
       },
       {
        "type": "null"
       }
      ],
      "title": "Model Ref"
     },
     "capabilities": {
      "$ref": "#/components/schemas/RuntimeCapabilities",
      "default": {
       "coding": false,
       "tools": [],
       "local": false
      }
     }
    },
    "additionalProperties": false,
    "type": "object",
    "title": "RuntimeTarget",
    "description": "A concrete, non-secret runtime choice.\n\nTwo shapes, never mixed at input (P6): either `runtime_id`\nreferences a registered runtime (canonical \u2014 identity, refs and\ncapabilities then come from the Registry) or the inline anchors\n(`machine_id`, `harness_ref`, `provider_ref`, `model_ref`,\n`capabilities`) carry the choice directly (P4 legacy, still valid).\nThe input exclusivity is enforced service-side (`invalid_runtime_binding`):\nthe *resolved* effective target legitimately carries both the\n`runtime_id` (provenance) and the registry-resolved fields.\nOpen references only: an owned machine locus, abstract harness/provider/model\nlabels (same status as agent observability metadata \u2014 never a vendor\ncatalog, never a whitelist), and a capability snapshot used for\ncompatibility checks. No secret field exists on this model by\nconstruction: there is nowhere to put an API key, token or credential.\nAt least one anchor is required."
   },
   "SessionRuntimeOverride": {
    "properties": {
     "target_kind": {
      "$ref": "#/components/schemas/LibraryKind"
     },
     "target_stable_key": {
      "type": "string",
      "maxLength": 200,
      "minLength": 1,
      "title": "Target Stable Key"
     },
     "target": {
      "$ref": "#/components/schemas/RuntimeTarget"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "target_kind",
     "target_stable_key",
     "target"
    ],
    "title": "SessionRuntimeOverride",
    "description": "One ephemeral session-level runtime choice carried by a resolution\nrequest (P7). Same key shape as a stored binding\n(`target_kind`, `target_stable_key`) plus the concrete `target` \u2014 but\nnever persisted: the router forwards it as resolution context only\n(`session_overrides`), and the service validates it exactly like a\nstored choice (unknown machine 404, another user's machine 403)."
   },
   "Task": {
    "properties": {
     "created_at": {
      "type": "string",
      "format": "date-time",
      "title": "Created At"
     },
     "updated_at": {
      "type": "string",
      "format": "date-time",
      "title": "Updated At"
     },
     "version": {
      "type": "integer",
      "title": "Version"
     },
     "id": {
      "type": "string",
      "format": "uuid",
      "title": "Id"
     },
     "readable_id": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Readable Id"
     },
     "project_id": {
      "type": "string",
      "format": "uuid",
      "title": "Project Id"
     },
     "title": {
      "type": "string",
      "title": "Title"
     },
     "description": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Description"
     },
     "status": {
      "$ref": "#/components/schemas/TaskStatus",
      "default": "created"
     },
     "claimed_by_machine_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Claimed By Machine Id"
     },
     "claimed_by_agent_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Claimed By Agent Id"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "created_at",
     "updated_at",
     "version",
     "id",
     "project_id",
     "title"
    ],
    "title": "Task"
   },
   "TaskCreate": {
    "properties": {
     "project_id": {
      "type": "string",
      "format": "uuid",
      "title": "Project Id"
     },
     "title": {
      "type": "string",
      "title": "Title"
     },
     "description": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Description"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "project_id",
     "title"
    ],
    "title": "TaskCreate"
   },
   "TaskStatus": {
    "type": "string",
    "enum": [
     "created",
     "in_progress",
     "blocked",
     "completed"
    ],
    "title": "TaskStatus",
    "description": "Task lifecycle. Claiming a task moves it to `in_progress`; releasing\nclears the holder without changing the status."
   },
   "TaskUpdate": {
    "properties": {
     "title": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Title"
     },
     "description": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Description"
     },
     "status": {
      "anyOf": [
       {
        "$ref": "#/components/schemas/TaskStatus"
       },
       {
        "type": "null"
       }
      ]
     }
    },
    "additionalProperties": false,
    "type": "object",
    "title": "TaskUpdate"
   },
   "Timeline": {
    "properties": {
     "project_id": {
      "type": "string",
      "format": "uuid",
      "title": "Project Id"
     },
     "days": {
      "items": {
       "$ref": "#/components/schemas/TimelineDay"
      },
      "type": "array",
      "title": "Days"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "project_id",
     "days"
    ],
    "title": "Timeline",
    "description": "Day-grouped project activity (newest day first, events ascending\nwithin a day) \u2014 unfiltered, unlike the Review Queue: this is\nhistory, not an actionable signal. Inherits `GET /events`'s \"not claimed\nexhaustive\" honesty since it reads the same underlying data."
   },
   "TimelineDay": {
    "properties": {
     "date": {
      "type": "string",
      "format": "date",
      "title": "Date"
     },
     "events": {
      "items": {
       "$ref": "#/components/schemas/EventEnvelope"
      },
      "type": "array",
      "title": "Events"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "date",
     "events"
    ],
    "title": "TimelineDay"
   },
   "TokenRequest": {
    "properties": {
     "email": {
      "type": "string",
      "title": "Email",
      "examples": [
       "admin@example.com"
      ]
     },
     "password": {
      "type": "string",
      "title": "Password",
      "examples": [
       "super-secret"
      ]
     }
    },
    "type": "object",
    "required": [
     "email",
     "password"
    ],
    "title": "TokenRequest"
   },
   "TokenResponse": {
    "properties": {
     "access_token": {
      "type": "string",
      "title": "Access Token"
     },
     "token_type": {
      "type": "string",
      "title": "Token Type",
      "default": "bearer"
     }
    },
    "type": "object",
    "required": [
     "access_token"
    ],
    "title": "TokenResponse"
   },
   "Transfer": {
    "properties": {
     "id": {
      "type": "string",
      "format": "uuid",
      "title": "Id"
     },
     "transfer_code": {
      "type": "string",
      "title": "Transfer Code"
     },
     "sender_user_id": {
      "type": "string",
      "format": "uuid",
      "title": "Sender User Id"
     },
     "recipient_user_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Recipient User Id"
     },
     "project_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Project Id"
     },
     "task_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Task Id"
     },
     "build_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Build Id"
     },
     "category": {
      "$ref": "#/components/schemas/TransferCategory"
     },
     "filename": {
      "type": "string",
      "title": "Filename"
     },
     "object_key": {
      "type": "string",
      "title": "Object Key"
     },
     "content_type": {
      "type": "string",
      "title": "Content Type"
     },
     "size_bytes": {
      "type": "integer",
      "title": "Size Bytes"
     },
     "sha256": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Sha256"
     },
     "content_md5": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Content Md5"
     },
     "status": {
      "$ref": "#/components/schemas/TransferStatus",
      "default": "created"
     },
     "expires_at": {
      "anyOf": [
       {
        "type": "string",
        "format": "date-time"
       },
       {
        "type": "null"
       }
      ],
      "title": "Expires At"
     },
     "created_at": {
      "type": "string",
      "format": "date-time",
      "title": "Created At"
     },
     "uploaded_at": {
      "anyOf": [
       {
        "type": "string",
        "format": "date-time"
       },
       {
        "type": "null"
       }
      ],
      "title": "Uploaded At"
     },
     "downloaded_at": {
      "anyOf": [
       {
        "type": "string",
        "format": "date-time"
       },
       {
        "type": "null"
       }
      ],
      "title": "Downloaded At"
     },
     "deleted_at": {
      "anyOf": [
       {
        "type": "string",
        "format": "date-time"
       },
       {
        "type": "null"
       }
      ],
      "title": "Deleted At"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "id",
     "transfer_code",
     "sender_user_id",
     "category",
     "filename",
     "object_key",
     "content_type",
     "size_bytes",
     "created_at"
    ],
    "title": "Transfer",
    "description": "Complete transfer record. Every field is part of the stable shape \u2014\nreaders should ignore unknown future fields rather than reject them."
   },
   "TransferCategory": {
    "type": "string",
    "enum": [
     "temporary",
     "build",
     "asset",
     "raw_recording"
    ],
    "title": "TransferCategory",
    "description": "Retention classes: `temporary` (short-lived sharing), `build`\n(retained longer), `asset` (manual/long retention), `raw_recording`\n(local by default, not auto-uploaded)."
   },
   "TransferConsumption": {
    "properties": {
     "project_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Project Id"
     },
     "consumed_bytes": {
      "type": "integer",
      "title": "Consumed Bytes"
     },
     "quota_bytes": {
      "type": "integer",
      "title": "Quota Bytes"
     },
     "remaining_bytes": {
      "type": "integer",
      "title": "Remaining Bytes"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "consumed_bytes",
     "quota_bytes",
     "remaining_bytes"
    ],
    "title": "TransferConsumption"
   },
   "TransferCreate": {
    "properties": {
     "recipient_user_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Recipient User Id"
     },
     "project_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Project Id"
     },
     "task_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Task Id"
     },
     "build_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Build Id"
     },
     "category": {
      "$ref": "#/components/schemas/TransferCategory"
     },
     "filename": {
      "type": "string",
      "title": "Filename"
     },
     "content_type": {
      "type": "string",
      "title": "Content Type"
     },
     "size_bytes": {
      "type": "integer",
      "title": "Size Bytes"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "category",
     "filename",
     "content_type",
     "size_bytes"
    ],
    "title": "TransferCreate"
   },
   "TransferStatus": {
    "type": "string",
    "enum": [
     "created",
     "uploading",
     "ready",
     "downloaded",
     "expired",
     "deleted"
    ],
    "title": "TransferStatus",
    "description": "Transfer lifecycle, from declaration (`created`) to removal (`deleted`)."
   },
   "UploadCompleteRequest": {
    "properties": {
     "size_bytes": {
      "type": "integer",
      "title": "Size Bytes"
     },
     "sha256": {
      "type": "string",
      "title": "Sha256"
     },
     "upload_id": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Upload Id"
     },
     "parts": {
      "anyOf": [
       {
        "additionalProperties": {
         "type": "string"
        },
        "type": "object"
       },
       {
        "type": "null"
       }
      ],
      "title": "Parts"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "size_bytes",
     "sha256"
    ],
    "title": "UploadCompleteRequest",
    "description": "Multipart parts/upload_id are tracked client-side and handed back\nhere \u2014 the server never persists multipart-in-progress state.\n\n`size_bytes` must match the `Transfer.size_bytes` declared at creation\n(the value quota enforcement was computed against) and the real object\nsize in storage \u2014 a client cannot inflate the stored size past\ncreation-time quota checks by declaring a small size then completing\nwith a larger one. `sha256` is recorded as reported by the client: for\nthe single-upload path it is corroborated by the server-verified\n`content_md5`; for multipart it remains an unverified client claim."
   },
   "UploadInitiateRequest": {
    "properties": {
     "content_md5": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Content Md5"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "title": "UploadInitiateRequest",
    "description": "`content_md5` (base64-encoded MD5 of the whole file) is required for\nthe single-upload path: the server embeds it in the presigned URL, so\nobject storage itself rejects any byte mismatch at upload time \u2014 no\nbytes ever flow through the API process. Not used for the multipart\npath, where per-part integrity is enforced by storage's own part\nmatching instead."
   },
   "UploadInitiateResponse": {
    "properties": {
     "transfer_id": {
      "type": "string",
      "format": "uuid",
      "title": "Transfer Id"
     },
     "multipart": {
      "type": "boolean",
      "title": "Multipart"
     },
     "upload_url": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Upload Url"
     },
     "upload_id": {
      "anyOf": [
       {
        "type": "string"
       },
       {
        "type": "null"
       }
      ],
      "title": "Upload Id"
     },
     "part_urls": {
      "anyOf": [
       {
        "additionalProperties": {
         "type": "string"
        },
        "type": "object"
       },
       {
        "type": "null"
       }
      ],
      "title": "Part Urls"
     },
     "part_size_bytes": {
      "anyOf": [
       {
        "type": "integer"
       },
       {
        "type": "null"
       }
      ],
      "title": "Part Size Bytes"
     },
     "part_urls_expires_at": {
      "anyOf": [
       {
        "type": "string",
        "format": "date-time"
       },
       {
        "type": "null"
       }
      ],
      "title": "Part Urls Expires At"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "transfer_id",
     "multipart"
    ],
    "title": "UploadInitiateResponse",
    "description": "Small file: a single pre-signed PUT. Large file: multipart parts, each\nwith its own pre-signed URL. `part_urls_expires_at` (multipart only)\nlets the client refresh proactively before the URLs actually expire\nrather than only reacting to a 403 from storage."
   },
   "UploadPartsRefreshRequest": {
    "properties": {
     "upload_id": {
      "type": "string",
      "title": "Upload Id"
     },
     "part_size_bytes": {
      "anyOf": [
       {
        "type": "integer"
       },
       {
        "type": "null"
       }
      ],
      "title": "Part Size Bytes"
     },
     "part_numbers": {
      "anyOf": [
       {
        "items": {
         "type": "integer"
        },
        "type": "array"
       },
       {
        "type": "null"
       }
      ],
      "title": "Part Numbers"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "upload_id"
    ],
    "title": "UploadPartsRefreshRequest",
    "description": "Re-presign the still-missing parts of an in-progress multipart upload\nwhose cached URLs have expired \u2014 never re-presigns a part storage\nalready accepted. `part_size_bytes`, if given, must match the value\nfrom the original `initiate` response (`409 part_size_mismatch`\notherwise, guarding against a stale client resuming under a changed\nserver constant). `part_numbers`, if omitted, means \"every part not yet\nconfirmed by storage\"."
   },
   "UploadPartsRefreshResponse": {
    "properties": {
     "transfer_id": {
      "type": "string",
      "format": "uuid",
      "title": "Transfer Id"
     },
     "upload_id": {
      "type": "string",
      "title": "Upload Id"
     },
     "part_size_bytes": {
      "type": "integer",
      "title": "Part Size Bytes"
     },
     "part_urls": {
      "additionalProperties": {
       "type": "string"
      },
      "type": "object",
      "title": "Part Urls"
     },
     "uploaded_parts": {
      "additionalProperties": {
       "type": "string"
      },
      "type": "object",
      "title": "Uploaded Parts"
     },
     "expires_at": {
      "type": "string",
      "format": "date-time",
      "title": "Expires At"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "transfer_id",
     "upload_id",
     "part_size_bytes",
     "part_urls",
     "uploaded_parts",
     "expires_at"
    ],
    "title": "UploadPartsRefreshResponse",
    "description": "`uploaded_parts` is storage's own record (`ListParts`), the\nauthoritative source of which parts are actually durable \u2014 the client\nmust adopt it (a part PUT that succeeded but whose local SQLite write was\nlost is recovered here, not re-uploaded). `part_urls` carries only the\nparts still missing, never a part already in `uploaded_parts`."
   },
   "User": {
    "properties": {
     "created_at": {
      "type": "string",
      "format": "date-time",
      "title": "Created At"
     },
     "updated_at": {
      "type": "string",
      "format": "date-time",
      "title": "Updated At"
     },
     "version": {
      "type": "integer",
      "title": "Version"
     },
     "id": {
      "type": "string",
      "format": "uuid",
      "title": "Id"
     },
     "display_name": {
      "type": "string",
      "title": "Display Name"
     },
     "email": {
      "type": "string",
      "title": "Email"
     },
     "role": {
      "$ref": "#/components/schemas/Role"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "created_at",
     "updated_at",
     "version",
     "id",
     "display_name",
     "email",
     "role"
    ],
    "title": "User"
   },
   "UserCreate": {
    "properties": {
     "display_name": {
      "type": "string",
      "title": "Display Name"
     },
     "email": {
      "type": "string",
      "title": "Email"
     },
     "role": {
      "$ref": "#/components/schemas/Role",
      "default": "developer"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "display_name",
     "email"
    ],
    "title": "UserCreate",
    "description": "Admin-only, non-replayable \u2014 no `Idempotency-Key` support, unlike\ntask/claim/decision/transfer/project creation (a replayable user\ncreation would persist sensitive material)."
   },
   "ValidationError": {
    "properties": {
     "loc": {
      "items": {
       "anyOf": [
        {
         "type": "string"
        },
        {
         "type": "integer"
        }
       ]
      },
      "type": "array",
      "title": "Location"
     },
     "msg": {
      "type": "string",
      "title": "Message"
     },
     "type": {
      "type": "string",
      "title": "Error Type"
     },
     "input": {
      "title": "Input"
     },
     "ctx": {
      "type": "object",
      "title": "Context"
     }
    },
    "type": "object",
    "required": [
     "loc",
     "msg",
     "type"
    ],
    "title": "ValidationError"
   },
   "VersionOrigin": {
    "type": "string",
    "enum": [
     "lock",
     "active",
     "pin"
    ],
    "title": "VersionOrigin",
    "description": "How an effective version was picked: P2 shadowing selection yields\n`lock`/`active`; an exact version-pinned dependency edge yields `pin`.\nOnly the P5 resolved output emits `pin` \u2014 `LibraryResolution` (P2) still\nemits `lock`/`active` alone (P5, additive)."
   },
   "WorkSession": {
    "properties": {
     "id": {
      "type": "string",
      "format": "uuid",
      "title": "Id"
     },
     "task_id": {
      "type": "string",
      "format": "uuid",
      "title": "Task Id"
     },
     "machine_id": {
      "type": "string",
      "format": "uuid",
      "title": "Machine Id"
     },
     "agent_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Agent Id"
     },
     "started_at": {
      "type": "string",
      "format": "date-time",
      "title": "Started At"
     },
     "ended_at": {
      "anyOf": [
       {
        "type": "string",
        "format": "date-time"
       },
       {
        "type": "null"
       }
      ],
      "title": "Ended At"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "id",
     "task_id",
     "machine_id",
     "started_at"
    ],
    "title": "WorkSession"
   },
   "WorkSessionCreate": {
    "properties": {
     "task_id": {
      "type": "string",
      "format": "uuid",
      "title": "Task Id"
     },
     "machine_id": {
      "type": "string",
      "format": "uuid",
      "title": "Machine Id"
     },
     "agent_id": {
      "anyOf": [
       {
        "type": "string",
        "format": "uuid"
       },
       {
        "type": "null"
       }
      ],
      "title": "Agent Id"
     }
    },
    "additionalProperties": false,
    "type": "object",
    "required": [
     "task_id",
     "machine_id"
    ],
    "title": "WorkSessionCreate"
   }
  },
  "securitySchemes": {
   "MachineBearer": {
    "type": "http",
    "description": "Machine credential provisioned out of band. Send it as `Authorization: Bearer <machine-token>` on every request under `/api/v1`. The token is opaque: a missing, invalid or revoked credential returns 401. Unauthenticated operations are `GET /healthz`, `GET /metrics`, the human dashboard login `POST /auth/token`, and the HMAC-signed GitHub ingress `POST /github/webhook` (etape 9.1: signed, never Bearer).",
    "scheme": "bearer"
   }
  }
 },
 "tags": [
  {
   "name": "health",
   "description": "Liveness, metrics and dashboard login probes (unauthenticated)."
  },
  {
   "name": "projects",
   "description": "Project registry. Creating a project requires a privileged role; reading is open to any authenticated machine."
  },
  {
   "name": "tasks",
   "description": "Work items with optimistic-concurrency updates and machine claims."
  },
  {
   "name": "sessions",
   "description": "Work sessions tying a machine to a task over a time span."
  },
  {
   "name": "claims",
   "description": "Soft locks on resource paths (files, folders). A claim warns other machines through a conflict event \u2014 it never blocks a write, a Git operation, or a transfer."
  },
  {
   "name": "decisions",
   "description": "Recorded project decisions with stable human-readable ids."
  },
  {
   "name": "library",
   "description": "Reusable AI definitions (rules, skills, agent definitions, model profiles, workflows) with immutable versions, explicit activation and project locks. User-scope rows are owner-or-admin only."
  },
  {
   "name": "agents",
   "description": "Provenance identities attached to the caller's own authenticated machine. Registering an agent grants no permission and requires no prior knowledge: any writer may materialize its own identity here. Needed only to attribute AI work logs."
  },
  {
   "name": "ai-work",
   "description": "AI work ledger. Entries reference an agent attached to the caller's own machine; foreign or unknown agents are rejected. Review transitions to approved/rejected require a privileged role."
  },
  {
   "name": "review-queue",
   "description": "Aggregated view of AI work reviews, proposed decisions, and recent resource conflicts awaiting a human decision. Read-only; also serves as the notifications surface."
  },
  {
   "name": "timeline",
   "description": "Day-grouped project activity derived from the event stream. Read-only, unfiltered history \u2014 see review-queue for what needs action."
  },
  {
   "name": "github",
   "description": "GitHub ingress and wiring. The webhook endpoint is signed (X-Hub-Signature-256), never Bearer-authenticated; integrations and builds are read by any authenticated machine, written by privileged roles or the server itself."
  },
  {
   "name": "builds",
   "description": "CI builds observed on wired GitHub repositories. Server-written (webhook, reconcile worker); read-only over HTTP."
  },
  {
   "name": "producer",
   "description": "Studio Producer: bounded, deterministic analyses over a project's shared state. Synchronous in v1; never mutates tasks or claims."
  },
  {
   "name": "heartbeats",
   "description": "Machine presence. Any authenticated machine \u2014 including read-only ones \u2014 may heartbeat; status is derived server-side."
  },
  {
   "name": "events",
   "description": "Project activity feed. Every event carries a stable client-generated `event_id` (replay the same id, get the stored event, never a duplicate). Read history with `GET /events?since=` or subscribe live with `GET /events/stream` (Server-Sent Events, resumable). Identity fields are validated against the authenticated machine."
  },
  {
   "name": "transfers",
   "description": "File transfer coordination: metadata and short-lived signed URLs only, never file bytes. The client uploads and downloads directly to object storage with the returned URLs."
  },
  {
   "name": "machines",
   "description": "Machine provisioning (privileged role). The very first machine is created out of band."
  },
  {
   "name": "users",
   "description": "User provisioning (privileged role). The very first user is created out of band."
  },
  {
   "name": "runtime-bindings",
   "description": "Stored runtime choices (P4) for logical library keys. The router never recomputes precedence \u2014 it stores, reads and releases choices; selection happens in the services and the resolution engine. Session overrides are ephemeral and never stored here."
  },
  {
   "name": "runtimes",
   "description": "Declared runtimes (P6 registry, generic \u2014 never provider-specific). Register, read, update under optimistic concurrency, and logically revoke. No secret is ever accepted or stored."
  },
  {
   "name": "resolutions",
   "description": "Canonical full resolution of an `AgentDefinition` to its `ResolvedAgentDefinition` (P5 engine via `resolve_full`): effective definition, rules, skills, model profile, winning runtime, compatibility verdict and structured provenance. Pure read \u2014 no fallback, no persistence."
  }
 ]
}
